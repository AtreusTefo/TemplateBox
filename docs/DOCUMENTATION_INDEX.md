# TemplateBox Documentation Index

Start here: `docs/memory/PROJECT_STATUS.md` for current build/deploy state and operational knowledge — paste it into a new chat session for full context with no re-discovery needed. Requirements live in `PRD.md` at the repo root.

**Path convention:** since July 27, 2026 the deployable site lives in `site/`, which is the Netlify publish directory and therefore the web root. Documents written before that date use unprefixed paths — read `js/app.js` as `site/js/app.js`, `index.html` as `site/index.html`. Public URLs are unaffected: `/index.html` is still `/index.html`. See `docs/architecture/ARCHITECURE.md`.

## docs/memory/
- `PROJECT_STATUS.md` — current state, live infrastructure, Adsterra zone reference, solved issues, operational gotchas, and the pattern for adding a new template/editor. Kept up to date as the single source of truth for AI session handoff.

## docs/architecture/
- `ARCHITECURE.md` — folder structure and system layout reference: the `site/` publish directory versus the non-deployed repository root, what each page and script is for, and the extension points that must stay in sync

## docs/project/
- `AGILE_HIERACHY.md` — Scrum hierarchy and technology stack reference
- `UI_UX_AUDIT.md` — formal interface audit against the supplied reference sites: catalog preview fidelity, hero and trust-signal hierarchy, loading-interstitial honesty and perceived duration, editor first-run and persistence feedback, stylesheet token layer. Findings carry status and double as a tracker
- `SEO_AUDIT.md` — formal search-visibility audit: orphaned editor pages, one URL competing for six intents, JavaScript-only blog metadata, structured-data coverage, sitemap and robots defects, render-blocking third-party resources, plus the target query clusters for the content programme

## docs/implementation/
- `BLOG_SYSTEM_IMPLEMENTATION.md` — serverless blog: admin panel authoring workflow, block content model, size-aware Adsterra placement registry, export/publish flow
- `MOCKUP_GENERATOR_IMPLEMENTATION.md` — print-on-demand mockup generator: canvas-rendered product templates, color swatches, design upload/reposition, PNG export
- `PHOTO_MOCKUP_TEMPLATES_IMPLEMENTATION.md` — photographic mockup templates in the same editor: the three-layer Sandwich Method, why compositing order is declared per template, the data-only registry and coordinate-picker workflow for adding one, the dormant perspective-warp path, and the object-storage migration route
- `MOCKUP_EDITOR_MULTI_LAYER_UI.md` — the mockup editor's tool-panel rebuild: the ordered layer stack and what of it persists, why selection chrome lives on a second canvas so it cannot reach an exported PNG, the dependency-free HSV colour picker, warped templates compositing through a flattened sheet, and two `.sr-only` specificity traps that each caused a site-wide horizontal scroll
- `BUSINESS_DOCUMENT_BUILDER_IMPLEMENTATION.md` — receipts, invoices and employee warning notices: one form driving six documents, blank printable form mode, automatic totals and amount-in-words, hand-rolled jsPDF table/checkbox primitives, catalog variant hand-off
- `LANDING_PAGE_AD_PLACEMENT.md` — why the landing-page banner is not in the hero (it competes with the funnel into the monetized loading flow), where it went instead, the hero whitespace fix, and the extraction of `js/ads.js` as the single placement registry for all three page families
- `SITE_PUBLISH_DIRECTORY_MIGRATION.md` — the move into `site/`, the new `404.html`, the five-column footer restructure applied from a single source, and the static guides archive; includes the four-artifact publishing checklist and what was and was not verified
- `EDITOR_FORM_NAVIGATION.md` — why the long editor forms got a section jump list rather than tabs (tabs hide fields, break browser find, and collide with the mobile Edit/Preview tab row), the sticky preview pane that fixed the actual defect of losing the live preview while scrolling, the `align-self: start` grid detail that makes it work, and the print resets without which the sheet is clipped at one viewport height
- `EDITOR_PAGE_AD_PLACEMENT.md` — reverses "no ads on the editors, ever" on a dwell-time argument: one passive banner per editor across three mutually exclusive viewport bands (160x600 rail at 84rem+, 728x90 leaderboard 48-84rem, 320x50 anchor below 48rem), the sticky-export-bar collision the anchor caused and its two-rule fix, why iframe isolation is load-bearing on these pages specifically, why Native Banner is prohibited there, and the six documents corrected alongside
- `DARK_MODE.md` - the --l-*/--d-* source-pair token architecture and why nothing reads them directly, the 45 hardcoded colours the audit found and how each category was handled, why document sheets and print output stay light, the inline no-flash script and the three theme states, the drift guards for the two necessary duplications, and the mega-menu regression the toggle exposed
- `FEED_HOVER_ZOOM_HIDE_ON_SCROLL_ROUNDED_CARDS.md` - the hover zoom and why it needs a wrapper element rather than scaling the existing children, the one scroll-direction utility behind both the hiding tab bar and the mobile hiding header (and why the hidden state must be a transform, not display:none), the measurement showing the column count rather than the gap was making the cards read small, and the rounded-card exception to the flat-and-square theme with the three documents it contradicted
- `HOMEPAGE_FEED_REWORK.md` - the 5/4/3/2 column ladder measured against feed width rather than viewport, why per-category column counts are not buildable without restructuring the feed, the card title becoming the only anchor and the stacking-context bug that nearly broke every click, the two hover previews (design-on-product, and the poster composited into the wood-a4 frame with no canvas and no new assets -- the poster one was removed on August 23, 2026, see CATALOG_THUMBNAIL_ADMIN.md), the description removal with its SEO and search-index consequences, and the layout sequence that followed: per-card ratios for masonry, removed; then CSS multi-column replaced by CSS Grid, with the measurements showing why multi-column cannot align its rows and keep five columns at the same time
- `CATALOG_THUMBNAIL_ADMIN.md` — a second workflow in `admin.html`: attaching a required default thumbnail and an optional hover thumbnail to any homepage catalog card, and publishing both straight into the working copy. Records the File System Access path (Chromium only, why a page cannot reach the file system otherwise, the folder-shape guard, images-before-markup ordering, and the eleven cases the patcher was tested against), why the `index.html` edit is a byte splice of one element rather than a DOMParser round-trip that would rewrite all 47 KB, the comment masking that stops a commented-out card steering the splice, the verification that must pass before anything is written, why it exports artifacts rather than driving a catalog registry (the feed's card titles are the only crawlable links to the editors, and a browser cannot write into `site/assets/` regardless), the in-browser compressor that resizes and re-encodes any upload to 60 KB or less rather than rejecting it (with the PSNR measurements, the halving downscale, the WebP support test that checks the returned blob's type, and the transparency scan that stops JPEG returning a black outline), why the extension comes from the mime type rather than the file name, the preview reusing the feed's own markup so it cannot disagree with production, the two `[hidden]` overrides an author `display` defeats, and the inert document that stopped every Copy Markup fetching a 404 for the file it was describing
- `FIXED_FULL_HEIGHT_AD_RAIL.md` — the rail becomes a column fixed to the window's right edge on the homepage and all four editors, with the whole page inset to its left by one `padding-right` on `body` (the Photopea layout). Records the deliberate reversal of the sticky-rail decision, why the sticky header needs no rule of its own to inherit the inset, how the page width caps had to shed the reserved width instead of reserving it twice, measured pane and feed widths before and after, and the 455-property zero-difference comparison proving the layout is untouched when the ad script is blocked
- `SEARCH_PAGE.md` — `search.html` and `js/search.js`: the site's first search results surface, what the phone and tablet search control opens now that it is a link rather than a dead class toggle. Records why the templates are fetched out of `index.html` and imported as real nodes rather than duplicated into a third catalog list (and the runtime coupling that buys), the launch-flow trap that cards inserted after `initCatalog` are unbound unless `bindLaunchControls` is called over them — with the `window.TB` guard that is always false because `TB` is a lexical global — the browse rows riding on `.catalog-grid` so cloned cards keep their styling, the sticky field tracking `--header-h`, and the noindex/follow reasoning with no robots.txt Disallow
- `MOCKUP_BACKGROUND_COLOUR.md` — the mockup editor's Background panel: why eligibility is declared per template rather than detected from the base image's alpha (wood-a4's transparent print window IS its mask, so an alpha test would paint the chosen colour behind the poster), why the fill goes on the canvas rather than in CSS (the PNG export and the tray thumbnails both read the canvas), how it differs from the white `backing` paper inside a frame's window, the one-picker-two-instances refactor and the parts deliberately left outside the factory, the parse-and-re-derive validation on the stored value, and the assertion that was written and then removed because no mutation could make it fail
- `MOCKUP_EDITOR_NAVBAR.md` — the mockup editor's bar rebuilt into real navigation: wordmark, a Mockups dropdown and a search field from 75rem; all-templates icon, hamburger and search button below it. Records why the whole bar switches on ONE boundary (a second would put the CSS and the JS into disagreement about when the bar is collapsed, which is what produced the dead search control), why the dropdown items need no routing code of their own and what that costs the visitor (an interstitial per switch), the JavaScript-free GET form, the document-name input's removal in favour of the controls' Mockup Label, the download label hidden on phones to keep the sticky header at one row (measured: 141px against 85px), and the parity evidence that the other three editors are untouched
- `MOCKUP_WORKSPACE_REBUILD.md` — the mockup editor becomes a workspace: the model photograph gains the Background panel on measured evidence (a transparent surround, which is the opposite case to the wood frame's transparent print window), Add and Download become one action row above the control column, Download becomes a JPG/PNG panel whose three sizes are derived from each product's own canvas rather than a fixed ladder, the saved-mockups tray moves into a tab, both columns hold the viewport and scroll independently, and that tab becomes a set of saved mockups the visitor switches between -- a tile is the thumbnail alone, clicking it reopens an editable mockup rather than showing a picture, and a permanent remove control replaces the per-item Download. Records why a hardcoded size ladder cannot work here, why a tray entry copies state in and out while sharing only the layer bitmaps (share the object and the tab silently becomes several copies of the current render), why `object-fit: contain` would break every drag, the arithmetic behind the `--mockup-aspect` width cap, why JPG flattens onto white and says so, and how Transparent moved into the picker so removing the background swatch row did not strand the default state
- `MOCKUP_CONTROL_PANEL_TRIM.md` — the mockup editor's control column loses three standing hint paragraphs and the Colour panel's colorway swatch row, measured at 233.5px shorter at 1920px and 351.9px at 320px. Records why the Background panel's hint and quick-pick row were kept (Transparent is a state no hex in the popover can express), the swatch row becoming the popover's own hue track a second time rather than a lookalike -- one `hue`, one handler, two thumbs -- what the colorways lose with their buttons and what they do not, why a pointer-only strip takes nothing off the keyboard path, and the trusted-input rewrite of the colourway check that `setPointerCapture` forces
- `MOCKUP_ASSET_FOLDER_STRUCTURE.md` — the flat mockup asset and thumbnail directories replaced by one nested product taxonomy mirrored across both trees (apparel/t-shirts, apparel/hats/baseball-caps, print/posters-and-frames), with the factory now writing the operator's chosen category into all eight emitted map paths rather than the thumbnail alone. Records why segments omit the word "mockup", why `slug()` had to gain a path-aware `slugPath()` sibling (it would flatten the separators and emit a filename where a folder was meant), why the category carries no runtime meaning and is not the card's `data-category`, and why empty category folders are left absent from git rather than pinned with `.gitkeep` files that `site/` would serve as public URLs
- `PAPER_BAG_MOCKUP.md` — the fourth photographic mockup and the first that is not cloth: why the prompt had to specify a WHITE bag (the classifier gates on sat < 14 and kraft measures around 70, so a kraft photograph could not be derived at all, while dyeing white to kraft gets the same product), the two fold lines that bound the front panel and the absent gusset crease that makes the whole width one plane, why a nearly flat surface needs a LOWER displaceStrength rather than a higher one despite a gradient p99 of 9.94 against the shirt's 41.5, and why no grain map ships. Also records the connectivity defect it exposed — flooding the raw classified mask dropped 75% of the rope handles because 1-3px twist grooves read as the end of the region, invisible until someone picked a colour, and reported as success by a check that counted a band the rim alone satisfied — and the dilate-flood-intersect fix, measured at every radius against the cap photograph to prove the model's t-shirt stays excluded
- `TWO_ZONE_BUSINESS_CARD_MOCKUP.md` — two business cards on walnut, and the multi-zone support they forced: a template can now declare `warpZones` and each surface carries its own independent design stack, with a Front/Back switch that moves the editing focus while both cards always render. Records why one shading pass still covers both (the sheet is canvas-sized and the shader discards where it is empty), why BOTH cards had to be axis-aligned (the warp path is checked first and returns, so one tilted card would cost both their lighting), why the connected-region restriction had to gain multiple seeds (163px of bare wood between the cards means one seed keeps one card and silently leaves the other unshaded and undyed), why a light background would have made the base unusable on an opaque scene, and the PNG encoder learning to write RGB when nothing is transparent
- `ROLLUP_BANNER_MOCKUP.md` — a roll-up banner stand, and the first template to ship FOUR maps instead of seven: blank vinyl has no colour variant and a design covers the whole face, so nothing is left for a garment mask, a tone map or a grain map to serve. 1.5MB against the paper bag's 11.3MB, the first time this catalog's weight has gone down. Records why the prompt had to specify ANTHRACITE hardware rather than aluminium (brushed metal passes both classifier gates while being joined to the face, and would drag the median and specular ceiling that shade and light normalise against), why lightGain stayed 0.3 on a measurement rather than by copying (narrowest headroom in the catalog pushed navy's p95 to 81.6, but blue-identity loss is 0.00% at every gain through 0.4 — uniform brightening, not a hue wash), and the half-pixel `put` bug that turned gold into mint green by landing colours two bytes off, latent through three earlier templates because white survives the misalignment
- `HOODIE_MOCKUP.md` — a white hoodie on a model, the seventh photographic template and the first built after the colourway chips landed, so its heather fractions and grain map are reachable on day one. Records the two hazards kept out of the photograph rather than worked around — drawstrings would hang through the print zone and be painted over, and white joggers would have been the cap's white-tee problem except CONNECTED to the hem, where the region restriction could not have saved it (dark jeans measured 0.0% classified) — the kangaroo pocket seam as the strongest horizontal step in the image, the torso centreline taken from the sleeve seams rather than the frame because the sleeves run off both edges, and why displaceStrength 10 follows from fleece draping in broader folds than jersey. Also the first template since the shirt where the lightGain ceiling is real: 6.47% blue-identity loss at gain 1.0, 0.00% at 0.3
- `BUCKET_HAT_MOCKUP.md` — a white bucket hat on a model, and the first template whose classifier thresholds are TUNED PER IMAGE rather than left at the factory defaults. The hat's own saturation runs 12-16, so the default gate of 14 bisected the crown and left it 87.9% classified with holes through the print zone; raising saturation alone then pulled 5,930 forehead pixels in, and unlike the cap's separate white tee the connected-region restriction cannot drop them, because the brim rests on the forehead and the skin is genuinely connected. Luma is what separates them (hat p1 174 against face p50 131), and sat<20 with luma>165 is strictly better than the defaults on every axis at once. Also records why the seamless front panel the prompt demanded is what makes an axis-aligned zone legitimate at all, and why the print is deliberately small at about 11.7 x 6.9cm
- `INTERIOR_FRAME_MOCKUP_AND_FILL.md` — a framed poster in a styled room, the first template set in a scene rather than shot against nothing, and the two faults it exposed across templates already shipped. Adds `designScale`, so artwork that IS the product fills its zone instead of opening at the 0.75 that suits a chest print — counted per SURFACE, since on the two-card template the back card's first design is layer two overall and was arriving at 0.350. Records that every zone had been derived from the classified mask, which sits 3-9px inside the real edge wherever a light surface meets something dark, and that the true edge is found by walking luma OUTWARD from the centre (walking inward reads the props). Also why the exact trapezoid was tried and reverted: a non-rectangular zone takes the warp path, which removes every hit rect, and rotation exists ONLY as a canvas drag — so the warp does not degrade rotation, it removes it. Ships three maps with no `light` at all, on a measured 3.4 luma levels of headroom
- `BACK_VIEW_TSHIRT_MOCKUP.md` — the back of the white tee, and the first template that is a second VIEW of a product already in the catalog rather than a new product. It gets its own id and its own card because the registry keys templates by id and `data-doc` is what opens one, so a view without an id is unreachable. Records the clause that made a back view possible at all (short hair above the collar — anything on the neck sits INSIDE the print area and there is no occlusion mask, the same failure as the bag's handles and a hoodie's drawstrings), the two landmarks that prove the two photographs are the same garment at the same distance before the front's 34.8 px/in is reused as 69.6 (sleeve tip to sleeve tip agrees to 0.26%, the hem to 0.35in), why the back print sits HIGHER in frame than the front's despite both following a 3in rule (a crew neck's front scoop is 1.8in below the back collar seam), and the resolution correction behind displaceStrength 27: a Sobel measures luma change PER PIXEL, so this 2048-wide base reads half the gradient of the 1024-wide front for the same physical fold, and 17.34 is 34.7 in the front's coordinates against its 41.5. Also two measurement traps caught in the act — a mask-mirror centreline search whose window was entirely fabric at every candidate, so it read 0.00% mismatch everywhere and could not fail, and a single stray pixel at (591,1467) that understated the zone's left margin as 20px when closing 1-D specks shows 63
- `BACK_VIEW_HOODIE_MOCKUP.md` — the back of the white hoodie, the second view-pair. Two things the shirt pair did not have. First, the hood: it occupies the top of the print area, so the zone is bounded from ABOVE by a garment feature the way the front hoodie's is bounded from below by the kangaroo pocket — the hood's cast shadow bottoms out in a row-mean trough at y=560 (203.8 against a body reading 234-236) and clear back begins at y=590. Second, the two hoodie photographs are NOT at the same scale, so unlike the shirt nothing could be carried across: three rulers give 31.8, 30.5 and 35.3 px/in, the two hoodie-to-hoodie ones agree to 4% on different axes and the head-width one crossing two unrelated photographs is the outlier, so the scale is recorded as an ESTIMATE with an 11% band rather than a measurement. Also introduces the change that applies to every future template: displaceStrength set from the ZONE's gradient rather than the whole garment's, because a global p99 counts features the print never touches (here the hood roll and the sleeve seams) and under-bends the artwork. The table of peak offset delivered inside each zone is here, and it also settles why the back shirt's 27 gives half the front's fractional bend and is right to.
- `HANGER_TSHIRT_MOCKUP.md` — the plain product shot, a tee on a wooden hanger, and the first template that REPLACES a product rather than joining the catalog: the drawn vector `tshirt` is retired. Two findings worth knowing before shooting any product on a rack. First, chrome and white cotton are the SAME THING to the classifier — the rail's specular measured luma 248 at saturation 1, brighter and less saturated than the shirt's 232 at 7 — so no gate separates them (at luma 190 the rail still keeps 11,110 pixels while the shirt starts losing its own shading), and the connected-region fallback failed too because the sleeve hangs in front of the post, putting 37,873 rail pixels inside the shirt's own region. The rail had to come out of the IMAGE. Second, the wooden hanger the prompt insisted on is what saved the hanger itself: saturation 103, 0.097% of it classifies. Also records the first base built to exactly 4:5 so its thumbnail is a straight downscale, the only map in the catalog shipping below its own quality floor (weave 1.92 against a 2.0 threshold, and what that costs the heather colourways), and the four things retiring a drawn product touched — including why `drawTshirtBody` had to STAY (the drawn hoodie is built on it) and why `.mk-shape.tee` is not dead CSS.
- `error-fixes/MOCKUP_PRINT_ZONES_OVERHANGING_THEIR_SURFACE.md` — artwork rendering OUTSIDE the product it is printed on, found by audit while the suite was clean at 1269/0. On the interior frame, 6,341 pixels of artwork sat on the black border, because the aperture is a trapezoid (279..843 at the top, 273..849 at the bottom) and the zone must stay an axis-aligned rectangle or scale and rotate die in the perspective warp — fixed by declaring an `overlay` that redraws the frame OVER the artwork, which is what that key is for and needed no renderer change. On the roll-up banner, a 616x2 strip of the design floated below the vinyl, because the zone ended at y=1347 and the face ends at y=1345 (616/616 pixels white at 1345, 0/616 at 1346). Also records two measurements that looked like faults and were not: the business card's “impure” zone pixels are all inside the mask's one-pixel feather with none at alpha 0, and the banner's previously reported bare-vinyl strip no longer exists.
- `CATALOG_THUMBNAIL_CARD_FIT.md` — the Leaning Wood Frame card showed 73.6px of empty card because its thumbnails were 1000x1000 in a 4:5 window with object-fit: contain. Records why `contain` was NOT changed to `cover` (it would silently crop the edges of every future design) and the file was reshaped instead, the Fill/Fit control added to admin.html's intake, why Fit pads with transparency rather than a colour (a baked light pad is a pale band on every dark-theme card, permanently), the alreadyFits fast path that let a small square file through untouched, why sizes are not forced to 800x1000, the stylesheet comment that asserted every shipped thumbnail was already 4:5 when two were not, and the first version of the new check that measured the element box and so could not fail
- `RESUME_TEMPLATE_ENGINE_IMPLEMENTATION.md` — the descriptor engine behind the resume templates: one layout pass producing a display list of primitive drawing operations, and two painters (SVG preview, jsPDF export) that make no layout decision, so the two outputs cannot disagree. Records why text is measured through jsPDF even when only previewing, why the preview is SVG rather than HTML (SVG's `y` IS the baseline the display list carries), the points-on-a-595x842-grid unit contract, the measured fidelity limit against source artwork drawn with different font metrics, and the two defects the build exposed — contact rows that did not advance past the line just drawn, and `xml:space` set with `setAttribute` instead of `setAttributeNS`, which collapsed leading spaces in the preview only. Its "not yet wired into resume.html" status is superseded by the next entry
- `RULED_SERIF_CV_TEMPLATE.md` — a supplied Figma design shipped as the `ruled-serif` template, and the change that took the engine live: `resume.html` gains a template picker built from the registry, and engine templates now draw both its preview and its PDF. Records why two renderers are kept on purpose and the two places they meet, why `engineTemplate()` exists rather than `TBResume.byId()` (whose unknown-id fallback would render a retired template as some other design), the landmark-by-landmark drift table against the source SVG with every deviation accounted for, the deliberate refusal to reproduce the artwork's one undersized heading, why `bottom` is a reservation boundary rather than the last baseline (setting it to the baseline costs a whole line and paginates the document), the two backward-compatible engine changes this design needed — array `sub` lines and one shared entry-emptiness gate that stops typed data vanishing — the `data-templates` field toggling that keeps hidden fields collected, why the accent swatches stay live instead of the green being hard-coded, the jsPDF load-order race an end-of-body script loses to a deferred CDN tag, and why the three existing resume cards were deliberately left without presets (the suite keys cards by `data-doc`, so a shared one would collapse them to a single id)
- `FOURTH_RESUME_DESIGN_GREY_RAIL.md` — the two-column `grey-rail` template shipped after sitting built-but-internal since August 2, closing a product-integrity gap: three of the four Resumes cards carried no `data-doc` at all, so they launched whatever template was last used — three cards, three promises, one document, and non-deterministic once a second design existed. Records the four fields that were the only thing keeping it internal, the palette indirection added so a role may name `accent` (its rail and display ink are one colour, and five literal hex values had made the swatch row a dead control on it), the invariant that every `defaultAccent` must be a swatch on the row and dark enough for the rail's white text, why an unwired `education[].field` is safe rather than an oversight, why the existing card was repointed instead of a fifth being added, the miniature rebuilt to the descriptor's own 37.65% rail fraction, and the two-column ATS trade. Also records a live breakage found while verifying it: `poster.html` declared a wrong SRI hash for jsPDF, so every browser blocked the script and the editor told visitors to check their connection — PDF export there had been dead since commit `cc7acff`
- `CLASSIC_TEMPLATE_MIGRATION.md` — the resume editor's original layout moved onto the descriptor engine and its two hand-written renderers deleted, leaving one renderer for every template. Records the equivalence check run BEFORE anything was removed (HEAD's `buildPdf` pulled verbatim out of git and compared against the engine by parsing both produced PDFs' content streams — three cases, 0 text, 0 style and 0 horizontal differences, page breaks at the same content), why the only drift is vertical and why a uniform section rhythm is the better answer than the old writer's incidental one, the falsy-zero `indent` bug the diff caught that no eye would have (`0 || 8` is 8, so prose descriptions were indented and wrapped 8pt narrow), the three backward-compatible engine additions the template needed, the 237 lines of renderer and dead CSS removed, `classic` becoming a real registry id rather than a sentinel, the loss of any renderer that works without jsPDF and the message that now covers it, and the preview-pane height change that shows up in the suite's HEAD-parity section
- `RESUME_SHARED_FIELDS_AND_CONTACT_GLYPHS.md` — Professional Title and Languages drawn by all three resume templates rather than one each, new Projects and References sections on all three, the two-column template's contact glyphs redrawn without their disc, and click-to-edit extended to cover a group of runs. Records the two stale `data-templates` gates the audit found (`grey-rail` renders `experience[].place` and `education[].place` and marks its entry descriptions, and both were gated to Ruled Serif alone, so the template drew a field the form gave no way to fill), why an empty title leaves each layout byte-identical to what it was measured against, why Classic and grey-rail list languages as bullets while Ruled Serif keeps its meters (a bar is a graphic an ATS parser reads as nothing, and a meter track cannot read against every accent on the swatch row), the `entryList` provenance a field the form keeps as rows and the engine reads as one string needs, why the disc had to take a `knockout` colour with it, the handset sampled off an arc because a hand-written polygon approximates a curve at only one size, the display list's deliberate refusal to carry a rotate operation, the PDF content-stream check confirming the 34-vertex path, `multi` split out of `inline: false` so wrapped prose stops throwing the visitor back to the form, and the pre-existing measurement bug that split exposed — a synchronous re-render detaches the node the overlay is about to measure, so clicking one phrase straight after another opened a box 14px wide. Adding Projects and References then exposed that the engine had NO widow-and-orphan control at all -- a section reserved room for its heading assembly and nothing for what the heading introduced, so Classic ended a page on a bare `REFERENCES`; the reservation now adds the body's first line, READ from the body through `bodyFirstLine` rather than restated, and the fix was checked by neutralising it and watching the orphan come back. Also records why every reference field is optional by design, why grey-rail puts references in the main column rather than its 224pt rail, and how the sample was trimmed back to one page on two of the three templates -- with the warning that the fit is NOT linear, since Classic measured 98pt of apparently free page and still paginated because the last section's heading fell short of its reservation, and the measurement showing Ruled Serif is structurally two pages at any content volume
- `DOCS_EDITOR_BAR_MARK_AND_ACTIONS.md` — the receipts and invoices editor puts the site mark alone at the far left of its bar and moves Print and Clear Form up beside Download. Records the two defects the request exposed (the house icon rendered AFTER the document name, because `.doc-name` carries no `order` and so sorts ahead of the `order: 1` icon; and above 75rem the shared rule hid that icon for a wordmark this page does not have, leaving the left edge empty), why the mark is a new ungated class rather than a reuse of `.editor-brand` or an edit to the shared `.editor-home`, the `margin-left: auto` the field-less bar needs back because `justify-content: space-between` otherwise floated the name field 219px from the mark, why no second copy of the two buttons was left behind (both are bound by id), the 320px arithmetic that puts every label behind `.bar-label` below 48rem, and why `.preview-actions` stays as the ad anchor's lift target while the dead `.doc-actions` rules were deleted

- `STYLESHEET_STRUCTURE.md` — why the site keeps ONE global stylesheet rather than a CSS file per page or a folder per page (the folder half changes URLs: 19 absolute sitemap entries, an absolute canonical per page, 38 internal links to about.html alone, and a deliberate `cleanUrls: false`), the measurements behind that call (250 KB on disk, 53% of it comments, 51 KB brotli, cached once across a multi-page flow with no build step to reassemble a split), the twenty `[S01]`-`[S20]` section tags that replace line numbers because line numbers rot on the first edit, and the one exception that was made: `css/admin.css` for the two private operator pages. Records why the new sheet must load AFTER the global one (`.admin-thumb-preview` beats `.catalog-grid`'s column ladder by order, not specificity), why `.btn-small` and `.radio-option` stayed behind despite sitting in the admin block (both are used by public pages), that `tools/og-image.html` needs the link too, and the 743-rule zero-difference comparison proving the refactor changed nothing

## docs/error-fixes/
- `RESUME_PDF_RASTERIZED_TEXT_FIX.md` — html2pdf.js rasterized PDF text; replaced with jsPDF native text API
- `ADSTERRA_AD_CONFLICT_FIX.md` — Popunder foreground hijack and duplicate banner tag interference
- `LOADING_REDIRECT_STALL_FIX.md` — loading page countdown stalling at zero without redirecting
- `SOCIAL_BAR_NOT_DISPLAYING.md` — diagnosis confirming correct integration; non-display caused by frequency capping and page lifetime, not a defect
- `LOCAL_SERVE_CLEAN_URL_DROPS_TARGET_QUERY.md` — `npx serve .`'s default clean-URL redirect drops the loading page's `?target=` query, sending every editor launch to the fallback default during local testing only; fixed via `serve.json`
- `INTERNAL_FILES_PUBLICLY_SERVED.md` — `publish = "."` served the entire `docs/` tree and every instruction/spec file over HTTP, including the ad zone keys and analytics IDs in `PROJECT_STATUS.md`; fixed with 404 redirect rules, with the rejected `site/` publish-directory alternative recorded
- `EXPORTED_POST_PAGE_FOOTER_DRIFT.md` — generated blog post pages shipped the pre-July-25 footer (no landing-page columns, no social row) because the footer string in `js/admin.js` was never updated with the hand-maintained pages; fixed by extracting a single `FOOTER` constant
- `HOMEPAGE_GUIDE_CARD_MISSING_EXCERPT.md` — the homepage guides strip read `post.standfirst`, a field no post has, so every card silently dropped its excerpt; fixed by reading `post.description`
- `BLOG_POSTS_NOT_CRAWLABLE_WITHOUT_JAVASCRIPT.md` — every link to a post was created in JavaScript, leaving `sitemap.xml` as the only static route to `blog/<slug>.html`; fixed with a static guides archive on `blog.html`, generated by a new admin export
- `STATIC_POST_PAGES_SERVED_NO_ADS.md` — the generated article pages carried no ad hosts and did not load `js/blog.js`, so four live Adsterra zones were inert on every page readers reach; fixed by emitting the hosts in the generator and filling them from the same `AD_ZONES` registry, plus a shared in-content placement rule
- `AD_HOST_SHIPPED_WITHOUT_ITS_SCRIPT.md` — four pages carried the site-wide anchor host with no `js/ads.js` tag, because the rollout script's guard regex matched the filename inside the comment it had just inserted; the verification shared the same substring blind spot and passed. Also fixes `privacy.html` loading no JavaScript at all, which let its four `data-target` footer links skip the loading interstitial
- `LOCAL_INDEX_PAGE_BLANK_DIRECTORY_LISTING_INSTEAD_OF_HOMEPAGE.md` — `cleanUrls: false` also disables `serve`'s directory-index resolution, so the local root URL rendered a file listing rather than the homepage; fixed by consolidating into one root `serve.json` with `public`, a `/` rewrite and no directory listing
- `PHOTO_MOCKUP_OVERLAY_WASHES_OUT_DESIGN.md` — uploaded artwork rendered pale and milky inside a photographic mockup frame because the overlay is a near-white luminance map (alpha 193, mean luma 211) drawn at the canvas default `source-over`, which paints over the design instead of shading it; fixed with a per-template `multiply` blend, whitelisted in code because Canvas silently ignores an unrecognized composite operation. Includes how to pick the blend from an asset's appearance and a pixel-level test
- `MOCKUP_EDITOR_AUDIT_AUGUST_2026.md` — three faults found by auditing every template and the editor together. Every named colourway had been unreachable since the August 25 control-panel trim removed the swatch row: with no route in, the picker always set CUSTOM, so `original` was one-way and the `heather` branch could not execute at all — 2.3MB of grain maps downloaded on every visit and never sampled. The saved-mockups tray silently collapsed a two-card mockup onto card 1 because `copyLayers` was the one layer copy that did not carry `zone`. And the global layer cap left a multi-surface template showing an empty list with a disabled add but an enabled upload that dead-ended. Also raises the suite's 20s server wait to 60s, which had been reporting a merely slow `npx serve` as a dead one
- `R2_CUSTOM_DOMAIN_ON_APEX_TAKES_DOWN_SITE.md` — the July 30 total outage: an R2 bucket had `templatebox.win` bound as its Custom Domain, which takes over the whole hostname, so nothing reached Netlify and an empty bucket 404'd every path. Records the destructive second step (removing the binding deletes the apex DNS record, turning a 404 into NXDOMAIN), the DNS-only-then-proxy sequencing Netlify verification requires, the `Server: cloudflare` plus `x-nf-request-id` header pair that is the only real proof, stale negative DNS caching outlasting the fix, and the two dashboard suggestions to refuse
- `CATEGORY_TABS_HIDDEN_BEHIND_HEADER_ON_PHONES.md` — `.feed-tabs` sticks at a literal 76px while `.site-header` renders 145px at 360-480px and 201px at 320px, so the entire 45px tab row sat inside the header's box and was painted over by it: category filtering was invisible and untappable on every phone width whenever the header was showing and the page was scrolled. Records why the August 14 verification missed it (it measured the header-hidden state, where the offset and the hide transform cancel exactly, not the header-revealed-while-scrolled state), the measured header heights per width, and the fix: a `--header-h` custom property published from a `ResizeObserver` on the header, with the old literals kept as `var()` fallbacks so a JavaScript failure degrades to the previous behaviour
- `IOS_SAFARI_AUTOZOOM_ON_EDITOR_FIELDS.md` — 27 text-entry fields across the four editors computed below 16px, so iOS Safari zoomed the viewport in on focus and never zoomed back out: `.doc-name` at 15px on all four, the poster text/Advanced/Download toolbars at 13px, the mockup colour and scale inputs at 12-13px. Records why the viewport meta tag was not at fault, why `maximum-scale=1` is the wrong fix (WCAG 1.4.4), the phone-scoped 16px rule, and the 168-combination overflow sweep proving the widened controls introduced no horizontal overflow
- `PUBLISH_FAILED_ON_A_STALE_DIRECTORY_HANDLE.md` — publishing aborted with "state cached in an interface object ... had changed since it was read from disk". Chrome caches filesystem state on a directory handle, and both publish routines write into a directory and then resolve more paths through that same long-lived handle. Records why it cannot reproduce against an origin-private directory (which is all the test harness can reach, so the code was tested against a filesystem that cannot exhibit the failure), the one-retry wrapper around every operation, why one retry rather than a loop, and the connect-time probe that used to report a transient stale handle as "that folder is not the site directory"
- `PUBLISHING_ONE_THUMBNAIL_DELETED_THE_OTHER.md` — the thumbnail workspace was write-only, so a card that ships two images presented an empty form; uploading one and publishing stripped the hover `<img>` and deleted its file on a fully successful run. Records why every downstream step was individually correct, the hydration that makes the panel read the card's current state before offering to replace it, and the explicit-removal flag that separates "I did not upload one" from "this card should not have one"
- `PUBLISH_DELETED_THUMBNAILS_BEFORE_REWRITING_MARKUP.md` — admin.html's Publish deleted the superseded thumbnails before rewriting `index.html`; the rewrite then failed and the homepage was left pointing at files that had just been removed, rendering a broken-image icon on a live card. Records why the existing "images first, markup last" ordering was right about the writes and silently wrong about the delete, the mtime evidence that pinned the sequence, the reordering that makes any refusal non-destructive, and the new static check that every local `<img src>` must exist on disk
- `POPUNDER_HIJACKS_ALL_PAGE_CLICKS_FIX.md` — the Pop-Under's document-level handler observes every homepage click, so popup-blocked visitors were redirected off-site from pills, nav links and even launch buttons; fixed with an inline window-capture click shield on index.html (only `[data-target]` clicks reach the ad script), delegated pill/discard handlers, and a re-issuing launch navigation watchdog; revised July 28 with a transparent-overlay neutralizer after the shield alone left the page inert against the real ad script, whose full-viewport click-capture layer is removed only from inside the document-level listener the shield starves
- `HEADER_SEARCH_BUTTON_DEAD_ON_PHONES_AND_TABLETS.md` — the header search control was visible and tappable from 361px to 992px and did nothing at all: the rule revealing the hidden field sat inside a 22.5rem media query while the `display: none` it was undoing was scoped to 62rem, so on every phone wider than an iPhone SE and every tablet below 992px it toggled a class that matched nothing. Records why 360px — the width anyone tests first — worked perfectly, why the fix is a real page rather than a repaired reveal, the two dead rules deleted with it, and the outcome-shaped regression check (click it, require a focused usable field at the other end) with its mutation results
- `EDITOR_NAME_FIELD_NEVER_REACHED_THE_EXPORT.md` — naming a document, resume or mockup had no effect on three of the four editors: the value was typed, sanitized, stored, restored on the next visit, and never read by the code that names the file (the resume exported from the person's name, the business document from its type and recipient, the mockup from a literal). Records the one `TB.fileSlug` that replaced three near-identical regex chains and why it must decode entities first ("Ada's CV" would export as `ada39s-cv.pdf`), the typed-name-wins rule with the old fallbacks kept for untouched fields, and the two interception points the regression check needs — jsPDF's save is not on the prototype and does not download through an anchor, so the constructor has to be wrapped

## Maintenance notes not covered by a dedicated document
- Publishing a blog post is a four-artifact operation: export `js/blog-data.js` from `admin.html`, export the post pages into `site/blog/`, paste Copy Guides Archive over the `<ul>` in `site/blog.html`, and add the new URL to `site/sitemap.xml`. See `docs/implementation/BLOG_SYSTEM_IMPLEMENTATION.md`, `BLOG_POSTS_NOT_CRAWLABLE_WITHOUT_JAVASCRIPT.md`, and the July 26 entry in `PROJECT_STATUS.md`.
- The catalog CTAs must stay as anchors, not buttons. Converting them back removes the only crawlable internal links to the editor pages. See `SEO_AUDIT.md` finding 2.1.
- Publishing a blog post is a one-click operation on Chrome and Edge: connect the project folder once, then Publish to Project writes `js/blog-data.js`, every `site/blog/<slug>.html`, the guides archive in `site/blog.html` and the post URLs in `site/sitemap.xml`, and removes the pages of deleted or hidden posts. The four Download/Copy buttons remain the whole workflow on Firefox and Safari and with no folder connected. Committing and pushing is always manual. See `docs/implementation/BLOG_SYSTEM_IMPLEMENTATION.md`.
- Both image intakes in `admin.html` run through one pipeline, `site/js/admin-image.js`: the catalog thumbnails at 60 KB / 1000px reshaped to 4:5, the blog covers at 120 KB / 1600px with no reshape. Change the mechanism there, not in a caller. The cover budget is set by `blog-data.js` inlining covers as data URIs, so every visitor to the blog index pays for every cover.
- Catalog card thumbnails are attached in `admin.html`'s Catalog Thumbnails panel. On Chrome or Edge, connect the `site` folder once and Publish writes the images and edits `site/index.html` for you; on Firefox and Safari, Download the two renamed files and paste the markup block by hand. Deploying is manual either way — review with `git diff`, then commit and push. The default thumbnail is required and the hover thumbnail is optional; a card with only the first is a complete card. The panel's item list is hardcoded in `site/js/admin.js` because the feed has no data file — add a card in both places. `tests/verify-layout.js` asserts the two agree (six checks, section 1j), so a drift fails the suite rather than surfacing as a wrongly generated card. See `docs/implementation/CATALOG_THUMBNAIL_ADMIN.md`.
- Adding a photographic mockup template is a data-only change: assets into `site/assets/mockups/<category>/` (URL-safe names, taxonomy in `MOCKUP_ASSET_FOLDER_STRUCTURE.md`), corners from `site/tools/mockup-admin.html`, one entry in `site/js/mockup-templates.js`, one card in `site/index.html`, one generated thumbnail, and the hero template count. No engine edits. See `docs/implementation/PHOTO_MOCKUP_TEMPLATES_IMPLEMENTATION.md`.
- The site footer is hand-maintained across every public content page plus one generator (`FOOTER` in `site/js/admin.js` for the exported blog posts). It is deliberately absent from `admin.html` (private tool), the four editors, and `loading.html` (transient/workspace pages where it added scroll length without a navigation purpose). A footer change means editing every page that still carries it and re-exporting `site/blog/`. Apply it from one script rather than by hand — see `EXPORTED_POST_PAGE_FOOTER_DRIFT.md`.
- `index.html` carried no banner at all until August 6, 2026; it now carries the same rail the editors do (and only that, plus the phone anchor) — active formats there remain banned outright. Editors carry passive banners only, selected by viewport band, up to three stacked 300x250 units on very wide screens; no active format, and Native Banner is prohibited there because it would run third-party code in the same document as the visitor's typed content. The rail is one column fixed to the window's right edge, with the page inset by one body padding that is only applied once a banner has actually filled. See `EDITOR_PAGE_AD_PLACEMENT.md` and `FIXED_FULL_HEIGHT_AD_RAIL.md`.
- Every ad placement on the site lives in `AD_ZONES` in `site/js/ads.js` — never copy the table into a second file. Pages with hosts in static markup opt into auto-mounting with `data-ads-static` on `<main>`; pages that render their own placements must not carry it, or impressions double-count. Indexable content pages carry passive banners only: no Popunder, no Social Bar. Article pages carry passive banners only: no Popunder, no Social Bar on indexable content. After changing `buildPostPage()`, re-export the post pages or the deployed ones keep the old shell. See `STATIC_POST_PAGES_SERVED_NO_ADS.md`.
- Only deployables belong in `site/`. It is the publish directory and is served verbatim, so a note or spec placed there becomes a public URL; anything outside it is never uploaded. See `INTERNAL_FILES_PUBLICLY_SERVED.md`.
- Run `node tests/verify-layout.js` before deploying. Nothing runs it automatically. It covers ad wiring, selector hooks, the fixed rail's inset contracts across five pages and ten widths, the launch flow, and an ads-blocked comparison against the last commit. See `RUNNING_THE_VERIFICATION_SUITE.md`. **Two test files this project's notes once described, `test-home.js` and `verify-site.js`, never existed** - if a guarantee is written down as tested, check that a check exists.
- Local testing is `npx serve` from the repository root, where `serve.json` points the server at `site/`. Not from inside `site/`, and never with `cleanUrls: false` alone — see `LOCAL_INDEX_PAGE_BLANK_DIRECTORY_LISTING_INSTEAD_OF_HOMEPAGE.md`.
- Click handlers on index.html must be window-capture delegated, not element-level: the ad click shield in the page head stops non-launch clicks from propagating below `window`, so an element-level listener there never fires. Launch controls (`[data-target]`) are the one exemption. New external links on index.html need their host added to the shield's allowlist. See `POPUNDER_HIJACKS_ALL_PAGE_CLICKS_FIX.md`.

## docs/guides/
- `RUNNING_THE_VERIFICATION_SUITE.md` - how to run `tests/verify-layout.js`, what each of its four sections checks and which real failure each one exists to catch, why the launch-flow checks must use trusted input rather than synthetic events, and the two rules for adding a check (assert the exact contract; prove the check fails before trusting it)

## docs/daily-reports/
- `2026-07-27_DAILY_REPORT.md` — structure review and publish-directory migration into `site/`, internal files no longer served, blog posts made crawlable, `404.html`, five-column footer, the "index page is blank" diagnosis, and both ad-monetization gaps (article pages serving zero ads, landing pages having none). Includes the verification table and the post-deploy checklist
- `2026-07-28_DAILY_REPORT.md` — the homepage click-hijack defect closed in two passes, the second after deobfuscating the live Pop-Under script and finding its transparent capture layer; includes why the first fix shipped a worse defect, the Adsterra escalation evidence, and the rule that ad networks stay passive under automation
- `2026-07-30_DAILY_REPORT.md` — photographic mockup templates added to the mockup generator (Sandwich Method, registry-driven, coordinate picker tool), the overlay blend-mode defect that washes out artwork with no error reported, the headless file-upload verification technique, and the discovery that production `templatebox.win` is serving 404 from Cloudflare rather than Netlify
- `2026-08-17_DAILY_REPORT.md` — the mockup editor's single-design placement rebuilt into an ordered multi-layer stack (data model, persistence, warped-template sheet compositing), selection chrome moved to a second canvas so it structurally cannot reach an exported PNG, hand-drawn resize/rotate handle icons, a dependency-free HSV colour picker, two site-wide `.sr-only`/`[hidden]` specificity bugs found and fixed, and the `dev3` branch fast-forwarded back in sync with `dev2`
