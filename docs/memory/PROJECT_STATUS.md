# TemplateBox — Project Status and Session Handoff

Last updated: August 30, 2026 (**a supplied Figma CV design shipped as the `ruled-serif` template, the descriptor engine reached the live editor, and Classic was then migrated onto it so `js/resume.js` contains no renderer at all.** Three pieces of work in one pass; the last two are the first two entries in the decision log. In short: the engine built on August 2 had never been wired into `resume.html`, so every visitor got one hand-written renderer whose preview and PDF agreed about content and nothing else. The editor now has a registry-built template picker, a multi-page preview that shows pages as pages, and one renderer for all of them -- the hand-written pair was deleted only after an equivalence check against the actual PDF files showed 0 text, 0 style and 0 horizontal differences across three cases. That check also caught a falsy-zero bug (`0 || 8`) that no eye would have. Suite: 1171 passed, 1 failed -- section 4 only, which diffs against HEAD and reports any intentional layout change until it is committed.

Previously in the same pass: **a supplied Figma CV design shipped as the `ruled-serif` template, and the descriptor engine finally reached the live editor.** `resume.html` had never used `js/resume-engine.js`: the engine and its registry were built on August 2 and wired only into the internal harness at `tools/resume-template-preview.html`, so every visitor got the one hand-written renderer in `js/resume.js`. The editor now carries a **template picker built from the registry**, and an engine template draws both its preview and its PDF. **Classic is a sentinel id, not a registry entry** -- the hand-written renderer stays, because it renders every resume saved before templates existed, and migrating it owes an output-equivalence check first; the two paths meet at exactly two places, `renderPreview()` and the download handler. Measurements came from the **SVG**, the only one of the three supplied files carrying exact baselines. Four things a future session needs: the descriptor resolves every non-ink colour to `accent` rather than hard-coding the green, so the swatch row is not a dead control on this template, with `defaultAccent` applied on selection only; `bottom` in a descriptor is a RESERVATION boundary, not the last baseline, and setting it to the baseline costs a whole line and paginates the document; `js/resume.js` is an end-of-body script and so runs BEFORE the deferred jsPDF tag, which the engine needs to measure, so the first paint waits for `DOMContentLoaded`; and the three existing resume cards were deliberately left without `data-doc`, because the suite keys cards by `data-doc || slug(title)` and a shared `classic` preset would collapse them to one id. Two backward-compatible engine changes were needed and both fixed real problems: array `sub` lines for three-baseline entry heads, and one shared entry-emptiness gate that stops an entry with a role but no company vanishing with the visitor's text in it. Suite: 1171 passed, 1 failed -- section 4 only, which diffs the tree against HEAD and reports any intentional layout change until it is committed. See the first entry in the decision log and `docs/implementation/RULED_SERIF_CV_TEMPLATE.md`.)

Previously updated: August 26, 2026 (**the receipts and invoices editor's bar: the site mark alone at the far left, ahead of the document name, and Print and Clear Form moved up beside Download.** Both halves fixed a defect rather than a preference: the house icon had been rendering AFTER the name field at every width, because `.doc-name` carries no `order` and so sorts ahead of the icon's `order: 1`; and above 75rem the shared rule hid that icon to make room for a wordmark this page does not have, leaving the left edge empty. Print and Clear Form MOVED rather than being copied -- both are bound by id -- and every label in the bar now hides below 48rem so the sticky header stays at one row. `.preview-actions` stays as the ad anchor's lift target with only the export note in it. resume, poster and mockup are untouched. See the first entry in the decision log and `docs/implementation/DOCS_EDITOR_BAR_MARK_AND_ACTIONS.md`.)

Previously updated: August 24, 2026 (**five changes in one pass: a real search page, a background colour for blank-background mockups, navigation in the mockup editor's bar, and catalog thumbnails that fill their card.** The header search control had been inert from 361px to 992px -- the rule revealing the hidden field sat inside a 22.5rem media query while the `display: none` it undid was scoped to 62rem, so on every phone wider than an iPhone SE and every tablet below 992px it toggled a class that matched nothing; it only ever worked at 360px. It is a link to the new `site/search.html` now, which searches templates AND guides and shows category browse rows when its field is empty. The mockup editor gained a Background panel (declared per template, never inferred from the base image's alpha) and a rebuilt bar carrying a wordmark, a Mockups dropdown and a search field above 75rem, an all-templates icon, a hamburger and a search button below it, with the document-name input removed in favour of the controls' Mockup Label. Three catalog thumbnails were off-ratio -- the Leaning Wood Frame pair at 1000x1000 left 73.6px of a 358px card empty -- and `admin.html`'s intake now reshapes every upload to the card's 4:5 window. See the first five entries in the decision log.)

Previously updated: August 22, 2026 (**`admin.html` gained a second workflow: Catalog Thumbnails.** Any of the eighteen homepage cards can now be given a default thumbnail and, optionally, a hover thumbnail, previewed in the feed's own markup and exported as two renamed image files plus the markup block to paste into `index.html`. It exports artifacts rather than driving a catalog registry, deliberately -- the card titles are the only crawlable links to the editors, and a browser cannot write into `site/assets/` in any case. Nothing about the public site changed except one stale card count. See the first entry in the decision log and `docs/implementation/CATALOG_THUMBNAIL_ADMIN.md`.)

Previously updated: August 20, 2026 (**tablets carried no advertising at all** -- every non-editor page family showed nothing from 769px to 1199px, because the anchor's ceiling stopped at 48rem and the rails do not start until 75rem. Both anchors now run to 74.9375rem so the bands meet exactly; `loading.html` shows both of its banners side by side on tablet and gains the anchor; and a `has-site-anchor` reservation sized for a retired 728x90 was over-reserving 116px under a 50px bar. `tests/verify-layout.js`'s own `RAIL_GAP` table had been asserting the hole was correct. See the first entry under Open Items.

Same day, earlier: responsive audit across phone, tablet, laptop and desktop. Two real defects fixed: the category tabs were painted over by the header on every phone width -- the header renders 145-201px there against the 76px the tabs reserved, so filtering was invisible and untappable, and sticky offsets now read a measured `--header-h` rather than a literal; and 27 editor fields computed under 16px, which makes iOS Safari zoom the page in on focus and never zoom back out. Ad-band exclusivity, the rail inset and horizontal overflow were all measured and are correct as documented. See the responsive-audit entry at the top of Open Items, plus `docs/error-fixes/CATEGORY_TABS_HIDDEN_BEHIND_HEADER_ON_PHONES.md` and `IOS_SAFARI_AUTOZOOM_ON_EDITOR_FIELDS.md`.)

Previously updated: August 14, 2026 (the homepage header now hides on scroll-down at every width, not only on mobile, reversing an August 13 mobile-only decision at the owner's explicit request; the category tabs rise to close the gap exactly as they already did on mobile. See the August 14 entry in the decision log, directly above the August 13 entry it supersedes.)

Previously updated: August 11, 2026 (site-wide dark mode: an explicit toggle in the header, a warm near-black palette, and a no-flash inline script on all 24 themed pages. Document sheets and print output deliberately stay light. See the August 11 entry in the decision log and `docs/implementation/DARK_MODE.md`.)

Previously updated: August 10, 2026 (homepage feed: hover zoom on the cards, the category tabs and the mobile header hide on scroll-down, and the cards are bigger and rounded -- the first rounded geometry on the site, a deliberate exception to the theme, see the August 10 entries in the decision log and `docs/implementation/FEED_HOVER_ZOOM_HIDE_ON_SCROLL_ROUNDED_CARDS.md`. Earlier the same day the feed was reworked: a 5/4/3/2 column ladder, the card title is now the link and the visible "Open editor" button is gone, hover previews on the mockup and poster cards, all card descriptions removed. Per-card preview ratios were added the same day to make multi-column read as masonry, then removed at the owner's request, and CSS multi-column was then replaced by CSS Grid because the columns could not both align their rows and keep five of them -- see the August 10 feed entry in the decision log and `docs/implementation/HOMEPAGE_FEED_REWORK.md`. Earlier the same day, the ad rail became a fixed full-height column at the right edge of the window on the homepage and all four editors, with the whole page inset to its left by one body padding — the Photopea layout. This knowingly reverses the sticky-rail shape described below and a documented revert of the fixed shape; see the August 10 decision-log entry and `docs/implementation/FIXED_FULL_HEIGHT_AD_RAIL.md` before undoing it)

Previously updated: July 30, 2026 (photographic mockup templates added to the Product Mockup Generator via the three-layer "Sandwich Method"; first template is a leaning wood A4 frame, deployed and verified live. The same session found and fixed a total production outage: an R2 bucket had claimed the apex domain as its custom domain, so nothing reached Netlify — see Known Issues Already Solved and the new operational notes on domain routing)

**Path convention:** the deployable site now lives in `site/`, which is the Netlify publish directory and therefore the web root. Paths in this document below this line are mostly unprefixed for historical reasons — read `js/app.js` as `site/js/app.js`. Public URLs did not change.

## Purpose of This Document

This is a snapshot of everything built, deployed, and learned during the initial build-out and launch of TemplateBox. Paste this file (or its path) into a new chat session to get instant context without re-deriving decisions already made. For requirements and specification, see `PRD.md` at the repo root — this document covers implementation state and operational knowledge that PRD.md does not.

## What TemplateBox Is

A 100% client-side, serverless template personalization engine at templatebox.win. Four live tools, backed by nine search-intent landing pages, a guides blog, and About/Terms/Privacy pages. The tools: a CV/Resume builder, a Business Document builder (rent receipts, cash payment receipts, itemized business receipts, sales and cash receipt forms, invoices, employee warning notices), a Poster/Canvas creator, and a Product Mockup generator for print-on-demand sellers (t-shirt, hoodie, mug, packaging box). Monetized through a mandatory 10-second ad loading page between the catalog and the editor. Full requirements and design system live in `PRD.md` and `CLAUDE.md`.

## Live Infrastructure

| Layer | Provider | Status |
|---|---|---|
| Domain registrar + DNS | Cloudflare (`templatebox.win`) | Live, orange-cloud proxied |
| SSL/TLS | Cloudflare, mode Full (strict) | Active |
| Hosting | Netlify, auto-deploy from `main` | Live |
| Source control | GitHub: `AtreusTefo/TemplateBox` | Live, `main` is production |
| Analytics | Microsoft Clarity, project ID `xix7m2758f` | Installed on all 5 pages, confirmed no console errors |
| Search | Google Search Console, Domain property `templatebox.win` | Verified via DNS TXT; homepage indexed; sitemap submission pending first crawl (see Known Issues) |
| Ads | Adsterra | 2x Banner 300x250 (distinct zones), Social Bar, plus the banner registry in `js/ads.js` — see table below. **Pop-Under removed August 6, 2026** (was redirecting visitors off-site on ordinary clicks) |

DNS record shape in Cloudflare: `templatebox.win` and `www` are both `CNAME` → `templatebox.netlify.app`, proxied (orange cloud). No Netlify DNS zone is used — it was deliberately deleted because Cloudflare Registrar locks nameservers to Cloudflare, so Netlify DNS can never complete verification.

## Adsterra Zone Reference

| Placement | Location | Zone key / script ID |
|---|---|---|
| ~~Popunder~~ | ~~`index.html` `<head>`~~ | `pl30250761` — **REMOVED August 6, 2026.** Zone still exists in the Adsterra dashboard; the tag no longer ships. It was redirecting visitors to third-party sites on ordinary clicks, which costs more in return usage than the format earned |
| Banner 300x250, slot 1 | `loading.html` `#ad-banner-1` | `4a408738c2170da16b47c5ac05b3780a` |
| Banner 300x250, slot 2 | `loading.html` `#ad-banner-2` | `70d844a3963c8415efa49af391c897a0` (distinct zone, provisioned by Adsterra support on request) |
| Social Bar | `loading.html`, directly before `</body>` | `pl30250765` |
| Banner 300x250 (reused zone 1) | `blog/<slug>.html` in-content break, host in generated markup, via `AD_ZONES.inContent` in `js/ads.js` | `4a408738c2170da16b47c5ac05b3780a` |
| Banner 300x250 (reused zone 2) | `blog/<slug>.html` end-of-article, via `AD_ZONES.endOfArticle` | `70d844a3963c8415efa49af391c897a0` |
| Banner 728x90 leaderboard | `blog.html` + `blog/<slug>.html` top, desktop, via `AD_ZONES.leaderboard` | `7577a9abda8083816fafd71754b18205` |
| Banner 320x50 mobile leaderboard | Same hosts, chosen instead of 728x90 under 48rem at load, via `AD_ZONES.leaderboardMobile` | `101fe70128e51351589ecd23ab2d0e21` |
| Banner 160x600 skyscraper | `blog/<slug>.html` sticky rail and `blog.html` sidebar, viewports over 70rem, via `AD_ZONES.skyscraper` | `aaa51e997d5bd5badf6557a7773f78a6` |

| Banner 300x250 (reused `inContent` zone) | The nine `*-template.html` landing pages, between the body copy and the FAQ, via `TBAds` auto-mount | `4a408738c2170da16b47c5ac05b3780a` |

`post.html` still carries the same registry and its own leaderboard host, but it is a 301 source and the admin draft preview only, so it serves no meaningful impressions. The pages readers reach are `blog/<slug>.html`.

| Banner 160x600 skyscraper (reused zone) | The four editors, the fixed rail at the window right edge, viewports 75rem to 93rem, via `AD_ZONES.skyscraper` | `aaa51e997d5bd5badf6557a7773f78a6` |
| Banner 300x250 rail slot 1 (dedicated zone) | The four editors, top of the three-slot rail, viewports 93rem and above, via `AD_ZONES.editorRail1`. Provisioned Aug 3, 2026 specifically for this slot -- reports separately from every other 300x250 on the site | `3d08daa8e24f9416073d41bb566768bb` |
| Banner 300x250 rail slot 2 (dedicated zone) | Same rail, middle slot, via `AD_ZONES.editorRail2`. Also provisioned Aug 3, 2026, also fully separated | `67fd95399c1261e2f4ffbd1b284dd38d` |
| Banner 300x250 rail slot 3 (dedicated zone) | Same rail, bottom slot, via `AD_ZONES.editorRail3`. The initial ticket response repeated the existing `endOfArticle` key for this slot rather than issuing a new one; a follow-up delivered the genuinely new zone below, so all three rail slots now report independently of each other and of every other zone on the site | `0f6d3819d6704f2c657da28a4e25ae11` |
| Banner 728x90 (reused zone) | The four editors, **the fixed anchor at the foot of the viewport, 48rem to 75rem** — an in-flow leaderboard above the workspace until August 20, 2026, when it was retired for scrolling out of a minutes-long session. Same zone, same creative, now persistent. Also the tablet unit for the site-wide and homepage anchors from the same date, via `mountAnchorUnit` | `7577a9abda8083816fafd71754b18205` |
| Banner 320x50 (reused zone) | The four editors, fixed anchor at the foot of the viewport, **at or below 48rem only** — above that the same anchor host carries the 728x90 instead (see the row above). Chosen by `mountAnchorUnit`, which every anchor on the site routes through | `101fe70128e51351589ecd23ab2d0e21` |
| Banner 300x250 x3 (reused zones) | **`index.html`**, the fixed rail at the window's right edge, viewports 93rem and above, via `mountHomeAds()` in `js/ads.js`. Added August 6, 2026 — reverses the homepage no-ads rule. This is the editor rail: same `[data-ad-rail-slot]` markup, same three zones, and the same CSS rule rather than a copy of it. Homepage and editor impressions blend in these three zones' reporting; dedicated zones need an Adsterra ticket for a size already in use | `3d08daa8e24f9416073d41bb566768bb`, `67fd95399c1261e2f4ffbd1b284dd38d`, `0f6d3819d6704f2c657da28a4e25ae11` |
| Banner 160x600 (reused zone) | **`index.html`**, the same rail, viewports 75rem to 93rem. The band that makes the rail look like a rail on a normal laptop — see the note below before removing it. Reuses the blog/editor skyscraper zone | `aaa51e997d5bd5badf6557a7773f78a6` |

**The homepage rail IS the editor rail, sharing one CSS rule (August 9, 2026).** Asked for in those words after several intermediate shapes were tried and rejected — a height-tiered creative swap, then a `position: fixed` full-height column at the window edge with `body` padding reserving its width. **The fixed full-height column was reinstated on August 10, 2026 and is now the shipping shape** (see the decision-log entry for that date); what stayed dead from those experiments is the height-tiered creative swap and any second standalone `.home-rail` block, because a survivor would fight the shared rule silently. What stands:

- **`.editor-rail, .home-rail { ... }` is one selector list, not two similar blocks.** That is the only reading of "the same rail" that cannot drift apart later. Everything comes from there: `position: fixed` from `top: 0` to `bottom: 0` at `right: 0`, the width token, the column stacking and the internal scroll. The accepted trade is that on a short viewport the lower slots sit inside a scroller most visitors will not scroll — the same trade the editors already make.
- **Both of the editors' bands are kept: the 300x250 stack at 93rem and up, a single 160x600 from 75rem to 93rem.** The floors matched as of August 13, 2026 (both 75rem). They did differ (75rem home, 84rem editors, because below 84rem an editor's rail comes out of its fixed panes while the feed's masonry columns just reflow); the editors' floor was then lowered to match, knowingly accepting that pane cost, so a 1280px laptop shows the rail everywhere. **The 160x600 band was dropped once and had to be put straight back.** The arithmetic said it was unnecessary — at 1200px a 300px rail still leaves three columns of roughly 257px, wider than the 185px the phone layout ships — and the arithmetic was beside the point. Reported from a 1366px laptop as "this is not a side rail": a 300px unit at that width is as wide as a content column, so it stops reading as a rail beside the feed and reads as a fourth column of adverts. `tests/verify-layout.js` now asserts the mounted unit is narrower than one feed column at every width in the band. **Width that is affordable is not the same as width that looks right**, and no amount of column-width arithmetic will tell you which you have.
- **The homepage `display: none` gate is declared after the shared rule, deliberately.** Media queries carry no specificity. Written up in the homepage section — where it looks like it belongs — it would sit above the shared `display: flex` at identical specificity and lose, leaving the rail visible on every viewport it is meant to skip. `tests/verify-layout.js` asserts the source order.
| Banner 320x50 (reused zone) | **`index.html`**, fixed anchor at the foot of the viewport. **Under 75rem as of August 20, 2026, not under 48rem** — it now runs to 74.9375rem, one pixel below the rail's floor, so the two meet exactly and the homepage no longer shows nothing between 48rem and 75rem. Still mutually exclusive with the rail | `101fe70128e51351589ecd23ab2d0e21` |

Adult ads are toggled off for this site in the Adsterra dashboard.

**`index.html` carried zero ads from launch until August 6, 2026, and this document described that as non-negotiable.** It was reversed by the owner when the catalogue moved to a three-column masonry layout, which leaves real empty margin beside the content on a wide screen. What changed is the placement, not the reasoning: the original objection was that ad clutter on the indexed first-impression page carries SEO and trust risk, and a passive banner in the page's own margin — one that cannot navigate the tab, cannot cover content, and renders nothing at all below 75rem — is a different proposition from the Pop-Under that was removed the same day. **Active formats on `index.html` remain banned.** If homepage bounce rate or Search Console impressions move against you after this ships, the rail is one attribute to delete.

The editors carried none either until July 27, 2026, when that half of the rule was reversed on a dwell-time argument; they now carry passive banners selected by viewport band, up to three on very wide screens. The editors also carry no site footer — it was removed as unnecessary chrome on a workspace page, which means **the privacy policy and terms are not linked from any editor**; worth revisiting if that ever needs to be reachable from where visitors type personal data. See the decision log below and `docs/implementation/EDITOR_PAGE_AD_PLACEMENT.md`.

**`loading.html` also lost its footer and gained the site-wide anchor (August 2026).** The footer served no purpose on a ten-second, noindex intermediary — there is nowhere the links could send a visitor that outperforms waiting out the countdown. The anchor is a reversal of a documented exclusion in `js/ads.js`: it was originally left off specifically because a unit fixed to the foot of the viewport during a ten-second countdown reads as if it might cover the thing the visitor is waiting on. It was added back on the reasoning that `.has-site-anchor` reserves body padding *only once a banner has actually filled*, which is the same mechanism that already makes this placement safe on every other page carrying it — the anchor takes its place below the loading card, not over it. Verify this specifically on a real short viewport before trusting it; it has not been checked in a browser. The page's existing two 300x250 banners and the Social Bar are untouched; the anchor draws from `leaderboard`/`leaderboardMobile`, distinct zones from the inline banners' `inContent`/`endOfArticle` keys, so nothing on the page repeats a zone. One unverified interaction worth knowing about: the Social Bar is a self-injecting widget that positions itself independently per Adsterra's own instructions (see `SOCIAL_BAR_NOT_DISPLAYING.md`), and if it renders at the same screen edge as the fixed anchor the two could visually collide — low probability, since that document already establishes the Social Bar rarely displays at all on this page's short lifetime, but worth a look if both are ever seen firing together.

## File Map

Everything below lives under `site/`, the publish directory, except the last two entries.

```
404.html             Not-found page (noindex, follow). Netlify serves it for every unmatched path; carries the standard shell, Clarity, and a Popular Pages list of the nine landing pages and four editors
index.html          Pinterest-style feed (rebuilt August 9, 2026; reworked August 10): 16 template cards (3 resume, 6 business docs, 3 poster, 4 mockup) with filled CSS document miniatures in a CSS Grid of 4 columns at 64rem, 3 at 48rem and 2 below (the 5-column band was removed the same day: the cards read too small), one uniform 4/5 preview ratio (no masonry -- per-card ratios and CSS multi-column were both tried and both removed the same day), title-as-link cards carrying no visible CTA and no descriptions, hover previews (design-on-product for mockups; the posters had a wood-a4 frame composite until it was removed on August 23, 2026), sticky horizontally-scrollable category tabs, header search, mega-menu, continue-where-you-left-off mount, guides strip, WebApplication + Organization JSON-LD. Fixed full-height ad rail at the window right edge at 75rem+, with the page inset to its left; fixed anchor under 48rem. No site footer. The hero, trust band, visible H1 masthead and keyword link strip were all removed; the H1 remains as .sr-only, which is the page's only heading
loading.html         10s interstitial: progress bar + numeric readout, chosen-template name, honest status copy, 2 banner slots (isolated in srcdoc iframes), Social Bar script, site-wide anchor (320x50/728x90, added Aug 2026), navigation watchdog, dependency-free inline countdown/redirect fallback (activates only if js/app.js fails to take over). No site footer -- see decision log
*-template.html / poster-maker.html / tshirt-mockup-generator.html
                     Nine document landing pages, one per search intent, each with its own title/H1/canonical, body content, FAQPage + BreadcrumbList JSON-LD and a CTA into the correct editor variant. Generated once from a template; now plain static files, hand-edit directly
about.html / terms.html    Entity and trust pages (Organization schema, operator information, acceptable-use and liability terms)
blog/<slug>.html     Static per-post pages carrying real metadata in the served markup; generated by admin.html "Download Post Pages". Also carry the four blog ad hosts (leaderboard, in-content, end-of-article, 160x600 rail) as empty elements auto-filled at load by TBAds (js/ads.js) because <main> carries data-ads-static. They load only app.js and ads.js -- not blog.js, not blog-data.js
tools/og-image.html  Internal Canvas-based Open Graph card generator (noindex via netlify.toml)
resume.html + js/resume.js    CV builder: split-pane editor, template picker, accent swatches, localStorage binding, jsPDF native-text PDF export. Contains NO renderer since August 30, 2026 -- every template including Classic is a descriptor drawn by the engine below
js/resume-templates.js        Data-only resume template registry. `catalog: true` is what makes a template pickable by a visitor; unflagged entries are harness-only
js/resume-engine.js           TBResume: one layout pass producing a display list, painted to SVG (preview) and jsPDF (export) so the two cannot disagree
docs.html + js/docs.js        Business document builder: one form driving six documents (rent/payment receipts, itemized business + sales receipts, invoice, employee warning notice), blank-printable-form mode, automatic totals and amount-in-words, jsPDF native-text export, print stylesheet
poster.html + js/poster.js    Poster creator: HTML5 Canvas, image upload with mime validation, frame styles, PNG export
mockup.html + js/mockup.js    Product mockup generator: flat-vector Canvas product illustrations (t-shirt/hoodie/mug/box) plus photographic "Sandwich Method" templates (base photo, warped/placed design, shadow overlay), mime-validated design upload, pointer drag + scale placement, in-memory "My Mockups" tray, PNG export
js/mockup-templates.js  THE photographic mockup registry (window.TB_PHOTO_MOCKUPS): data-only entries (id, title, thumb, base, overlay, mode, warpZone). Adding a template touches this file plus one index.html card, no engine changes
js/vendor/glfx.js       Vendored glfx.js 0.0.4 (MIT) for 4-point perspective warp; lazy-loaded only if a non-rectangular warpZone renders, never requested otherwise
tools/mockup-admin.html Internal coordinate picker for new mockup templates (click 4 corners, copy registry entry); noindexed via the existing /tools/* header rule
assets/mockups/         Photographic template assets (<id>-base.png with transparent print window, <id>-overlay.png shadows)
assets/thumbnails/product-mockups/<category>/   Generated catalog thumbnails (~63KB 4:5 JPEGs). The supplied 1.4MB 2000px webp renders also live here but are deliberately unused
search.html + js/search.js   Search results surface (August 24, 2026), and what the phone/tablet search control and the mockup editor's field open. Filters templates and guides; shows category browse rows when the field is empty. Templates are the REAL index.html cards, fetched and imported with DOMParser/importNode rather than duplicated into a registry -- so js/search.js must call TB.bindLaunchControls over what it inserts, or every card bypasses loading.html. noindex/follow, not in sitemap.xml, X-Robots-Tag in netlify.toml, no robots.txt Disallow. Carries the content-rail and site-anchor hosts like every other content page
privacy.html          Compliance page
css/style.css         Single global stylesheet (Fabric Film Studio theme: cream/charcoal light palette plus a warm near-black dark palette from Aug 11 2026, Playfair Display + Inter). Colours live as --l-*/--d-* source pairs; rules read --color-* aliases only
js/app.js             Shared: sanitization firewall, localStorage wrappers, catalog filtering, launch/redirect flow, mobile tab switching
blog.html             Public blog index: JS-rendered cards from js/blog-data.js, plus the static <nav class="guide-archive"> list that is the only link to a post in the served markup (regenerate with admin.html "Copy Guides Archive"), dormant leaderboard ad host, Blog JSON-LD
post.html             Runtime post renderer (?slug=), now only a 301 source and the admin draft preview; same AD_ZONES registry, so it stays a working reference implementation
js/admin-image.js    THE in-browser image pipeline, shared by both halves of admin.html: mime gate, decode, optional reshape to a target ratio, halving downscale, transparency-aware format choice, and a binary search for the highest quality under a byte budget. The caller supplies the policy (budget, pixel cap, ratio), this supplies the mechanism -- catalog thumbnails ask for 60 KB at 1000px reshaped to 4:5, blog covers for 120 KB at 1600px with no reshape. Deliberately one module rather than two copies: this project has twice been bitten by duplicated code drifting invisibly
js/admin-fs.js       File System Access API wrapper used by admin.html alone: the project-folder handle, permission states, IndexedDB persistence, and read/write/delete/list primitives. Chromium only; every caller must keep working without it. Knows nothing about catalogs or blogs
admin.html + js/admin.js    Private authoring panel (noindex, robots-disallowed), two independent workflows in one page. Blog: localStorage workspace, exports js/blog-data.js, the post pages and the guides archive. Catalog Thumbnails (Aug 22, 2026): attaches a required default thumbnail and an optional hover thumbnail to any of the 18 homepage cards, previews the pair in the feed's own markup, and either publishes them straight into the working copy (Chromium, via js/admin-fs.js: writes the images and splices index.html, with verification before any write) or exports the two renamed image files plus the markup block to paste by hand. Separate IIFE, separate storage key (tb_admin_catalog_thumbs), separate exports -- the two halves share nothing but the page
js/ads.js             THE ad placement registry (AD_ZONES) and mounting for every surface. Extracted from js/blog.js when the landing pages became a third page family needing banners. Auto-mounts into static markup on pages whose <main> carries data-ads-static; blog.html and post.html mount through their renderers instead
js/blog.js            Shared blog library: block content model, XSS-safe rendering, AD_ZONES size-aware placement registry
js/blog-data.js       THE blog database: static window.TB_BLOG_POSTS array, generated by admin.html export, deployed as an asset
robots.txt / sitemap.xml    SEO; sitemap uses lastmod and omits priority (Google ignores priority, uses lastmod); loading.html and admin.html excluded from the sitemap and noindexed by response header rather than by a robots.txt Disallow (a Disallow prevents the crawler ever reading the page's own noindex)

../netlify.toml      Outside site/, read at deploy time and never served: publish = "site", empty build command, baseline security headers, X-Robots-Tag noindex on /admin.html, /loading.html and /tools/*, 301 from post.html?slug= to blog/<slug>.html; no internal-file 404 rules are needed any more; CSP deliberately omitted (ad domains not finalized enough to allowlist safely)
../serve.json        Outside site/: local-server config. public=site points serve at the publish directory, cleanUrls=false keeps ?target= query strings, a / -> /index.html rewrite undoes the directory-index resolution that cleanUrls=false disables, directoryListing=false makes wrong paths 404. Run `npx serve` from the repo root, never from inside site/. See LOCAL_INDEX_PAGE_BLANK_DIRECTORY_LISTING_INSTEAD_OF_HOMEPAGE.md
../.vscode/settings.json    Outside site/: roots VS Code Live Server at /site for the same reason
../docs/error-fixes/*.md    Debugged issues, see Known Issues Already Solved below
```

## The Monetized Flow (as implemented)

```
index.html card click
  -> js/app.js launchTemplate(): direct navigation to loading.html?target=...
     A modified click (ctrl/cmd/shift/alt/middle) opens that same interstitial
     in a NEW tab instead; the anchor's href points at the editor so crawlers
     see a real link, and the handler is what keeps human clicks monetized
  -> loading.html: 10s countdown, 2 banners + Social Bar render
  -> at 0: navigation watchdog re-issues location.replace(editorUrl) every 700ms
     until the page actually unloads, so ad-initiated navigations can't strand the user
  -> resume.html or poster.html: localStorage-backed editor carrying passive
     banners by viewport band (three 300x250 in the fixed rail at 93rem+, one
     160x600 in that same rail 75-93rem, 728x90 leaderboard 48-75rem, 320x50
     anchor below 48rem) and no active format. Editors carry no site footer
```

index.html's own click behaviours (the category tabs, the continue-strip discard button) are bound as window-capture delegated listeners in `js/app.js` rather than element-level listeners. This was originally forced: the click shield stopped non-launch clicks propagating below window, so an element listener would never have fired. The shield went with the Pop-Under on August 6, 2026 and the constraint no longer applies, but the pattern is still correct and was left alone. Background: `docs/error-fixes/POPUNDER_HIJACKS_ALL_PAGE_CLICKS_FIX.md`.

Those handlers key on the `data-filter` **attribute**, not on a wrapper class. Renaming the tab container `.filter-pills` to `.feed-tabs` during the August 9 rebuild silently killed category filtering, because three selectors in `js/app.js` still demanded the old class and nothing failed loudly. `tests/verify-layout.js` now reads every page and both scripts off disk and asserts that every hook a selector requires — class, id or `data-` attribute — actually exists in the markup. Note the correction: the filter handlers key on the `data-filter` **attribute**, so a check that scanned only class names would have sailed straight past this bug. It was written that way first and caught nothing until the mutation test exposed it.

`EDITOR_ROUTES` in `js/app.js` is the whitelist mapping `target` query values to editor pages — this is what makes the redirect immune to open-redirect tampering. `loading.html` carries a second, dependency-free copy of this same whitelist inline (see below) — **both must be updated when adding a new editor**, or the fallback path silently sends unrecognized/failed cases to the default editor instead of the new one.

An editor that serves several catalog cards (docs.html serves six) also needs a **variant hand-off**: `initCatalog()` writes the clicked card's `data-doc` value to `localStorage` under `tb_editor_preset`, and `TB.takePreset()` reads and clears it on arrival. The preset is never a route — the editor matches it against its own variant table, so a tampered value can only resolve to something that already ships. Clearing on read is what stops a later direct visit from re-applying a stale card choice over the visitor's saved work.

## Known Issues Already Solved (see docs/error-fixes/ for full write-ups)

- **R2_CUSTOM_DOMAIN_ON_APEX_TAKES_DOWN_SITE.md** (July 30, 2026) — the entire site returned 404 from Cloudflare, with no Netlify headers, rendering a storage "Object not found / Is this your bucket?" page while `templatebox.netlify.app` served 200. Cause: an R2 bucket (`product-mockups`) had `templatebox.win` — the apex, i.e. the website — bound as its Custom Domain. An R2 custom domain binds the *whole hostname* to the bucket, so nothing reached Netlify, and the bucket was empty. Uploading objects would not have helped: a bucket has no page routing and Cloudflare does not serve bucket listings at a domain root, so `/` 404s by design. **The remedy has a destructive second step:** removing the binding also deletes the DNS record it created, taking the site from 404 to NXDOMAIN, which reads as a worse failure — the apex `CNAME` to `templatebox.netlify.app` must then be recreated by hand, left DNS-only until Netlify verifies it, and only then set to Proxied. `www` also appeared broken throughout despite an intact record, because Netlify 301s it to the primary domain and the apex was what was failing. Resolved the same day; verified with `Server: cloudflare` and `x-nf-request-id` present on the same response.

- **RESUME_PDF_RASTERIZED_TEXT_FIX.md** — html2pdf.js rasterizes text (fails ATS parsing); replaced with jsPDF native `doc.text()` API. Verified with an automated PDF-stream inspection (real `Tj` text operators, no embedded images) plus a manual browser highlight-copy-paste test.
- **ADSTERRA_AD_CONFLICT_FIX.md** — Popunder's fallback redirect could hijack the foreground tab when popups were blocked (last navigation assignment wins); fixed with the 150ms deferred nav in `launchTemplate()`. Also: two banner tags sharing one page context clobbered each other's global `atOptions`; fixed by isolating each in its own `srcdoc` iframe. The deferral half of this fix was superseded July 27 (next entry); the srcdoc isolation stands.
- **POPUNDER_HIJACKS_ALL_PAGE_CLICKS_FIX.md** (July 27, 2026) — the hijack above was never launch-click-specific: the Popunder's document-level handler observes every click on index.html, so popup-blocked visitors were redirected off-site from filter pills (no competing navigation at all), nav links (coin-flip race) and even launch clicks (one-shot 150ms deferral loses to a fast-committing ad). Fixed with an inline window-capture click shield in index.html that starves the ad script of every non-`[data-target]` interaction and refuses injected foreign-origin anchors; pills and the continue-strip discard moved to window-capture delegation to survive the shield; `launchTemplate()` now re-issues its navigation every 700ms until unload (loading.html's watchdog pattern). Verified with a 16-check Playwright run against a simulated hijacking ad handler (attached earlier than the real one can be) with the ad redirect delayed 600ms to mirror a cross-origin round trip. Known cost: Clarity's document-level click capture on index.html is starved too; scroll/movement replay unaffected. Revised July 28, 2026: the shield alone bricked the page against the real ad script, which captures the first click with a transparent full-viewport layer (random id, z-index 2147483650) removed only from inside its own document-level listener -- the very listener the shield starves -- so no click or hover reached the page at all. An overlay neutralizer in the same inline block now forces pointer-events:none on any oversized fixed/absolute element the page does not ship (MutationObserver plus 700ms sweep, geometry-based because the id is random). Verified by reproducing the dead page with a faithful simulation of the decoded layer mechanism, then passing 9 overlay checks and the updated 17-check regression suite.
- **LOADING_REDIRECT_STALL_FIX.md** — same "last navigation wins" defect class, but spanning the whole loading.html page lifetime instead of one click; fixed with the persistent navigation watchdog described above.
- **SOCIAL_BAR_NOT_DISPLAYING.md** — not a defect. Full delivery chain verified (script serves, runtime domains resolve and respond). Non-display was per-visitor frequency capping plus the widget's animated entrance losing the race against the page's 10-second lifetime. Confirmed serving via Adsterra dashboard impression counts.
- **INTERNAL_FILES_PUBLICLY_SERVED.md** — `publish = "."` plus no build step meant the whole workspace was the web root: `docs/**` (including this file, with the Adsterra zone keys and Clarity ID), `PRD.md`, `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `README.md` and `.github/` all answered 200 and were crawlable. First fixed with `force = true` 404 redirect rules, then superseded the same day by moving the deployable site into `site/` and setting `publish = "site"`, so internal files are not in the deploy at all. Treat the previously-served contents as having been public.
- **STATIC_POST_PAGES_SERVED_NO_ADS.md** — the July 26 move to static post pages produced a generator shell with no ad hosts and no `js/blog.js` include, so four live Adsterra zones (leaderboard, in-content 300x250, end-of-article 300x250, 160x600 rail) served nothing on the pages readers actually reach; `post.html`, which did have them, had been 301'd away. Fixed by emitting the hosts in the generated markup and auto-filling them from the shared registry (`initStaticPost()` at first, replaced hours later by the generic `TBAds` auto-mount when the landing pages needed the same thing). Same generator and same class of omission as the footer drift below. Also extracted `adBreakIndex()` (now in `js/ads.js`) so the runtime and generated paths position the in-content break identically, and fixed that rule placing an ad between a heading and its own first paragraph.
- **BLOG_POSTS_NOT_CRAWLABLE_WITHOUT_JAVASCRIPT.md** — every link to a post was created in JavaScript (`initBlogIndex()` in `js/blog.js`, `initGuidesStrip()` in `js/app.js`), so the served markup of `blog.html` contained no post link and `sitemap.xml` was the only static route to `blog/<slug>.html`. Fixed with a visible `<nav class="guide-archive">` list on `blog.html`, generated by a new "Copy Guides Archive" export in `admin.html` so it cannot disagree with the data file or the exported pages. Same defect class as the button-to-anchor and static-post-page fixes of July 26.
- **EXPORTED_POST_PAGE_FOOTER_DRIFT.md** — pages exported by `admin.html` carried the pre-July-25 single-row footer, so post pages linked to none of the nine landing pages and showed no social row. The footer string in `js/admin.js` had not been updated alongside the 21 hand-maintained copies. Now a single `FOOTER` constant in `js/admin.js`; the one existing post page was brought in line and verified byte-identical to the generator output.
- **HOMEPAGE_GUIDE_CARD_MISSING_EXCERPT.md** — `initGuidesStrip()` read `post.standfirst`; the field is `description` (`standfirst` is the CSS class on a post page). Truthiness guard on a missing property, so every homepage guide card silently dropped its excerpt.
- **LOCAL_INDEX_PAGE_BLANK_DIRECTORY_LISTING_INSTEAD_OF_HOMEPAGE.md** — reported as "index page is blank." Not an application defect: `site/index.html` was verified intact. Two faults. (1) `{"cleanUrls": false}` in serve.json also disables serve's directory-index resolution, so `GET /` rendered a file listing instead of the homepage — a side effect present since July 20 and invisible until the site moved into `site/`. (2) The server was pointed at the repository root, and serve returns `site/index.html` for the URL `/site` without redirecting to `/site/`, so every relative asset resolved one level too high and 404'd. Fixed with one root `serve.json` carrying `public: "site"`, a `/` rewrite, `cleanUrls: false` and `directoryListing: false`, plus `.vscode/settings.json` rooting Live Server at `/site`. `site/serve.json` deleted, and with it the last non-deployable inside the publish directory.
- **LOCAL_SERVE_CLEAN_URL_DROPS_TARGET_QUERY.md** — not an application defect. `npx serve .` (CLAUDE.md's own recommended local test command) 301-redirects `*.html` requests to extensionless clean URLs by default, which drops the `?target=` query string; `loading.html` then falls back to `DEFAULT_TARGET` ("resume") and redirects to the wrong editor once the countdown finishes, which reads like "the countdown isn't working." Netlify has no such redirect, so production was never affected. Fixed by adding `serve.json` (`{"cleanUrls": false}`) at the repo root so local testing matches production. Worth ruling out first if a loading-page flow ever seems to land on the wrong editor again during local testing.

## Operational Knowledge Not Written Down Elsewhere

These cost real time to work out during setup and aren't captured in any other doc — worth reading before touching DNS, Search Console, or Adsterra again.

- **Cloudflare DNS + Netlify domain cutover order matters.** Add the custom domain in Netlify first; if Netlify shows "Netlify DNS propagating" it means it tried to create its own DNS zone — go to the Netlify team's Domains page and delete that DNS zone, then re-add the domain so it shows "Pending DNS verification" instead (external-DNS mode). Only then add the Cloudflare CNAME records.
- **SSL cert sequencing:** keep new DNS records gray-cloud (DNS only) until Netlify's Let's Encrypt certificate shows as issued in Domain management, only then flip to orange-cloud proxied. Proxying before the cert issues can block Netlify's verification. After flipping to proxied, set Cloudflare SSL/TLS to **Full (strict)** — leaving it on "Flexible" causes an infinite redirect loop against Netlify's forced HTTPS.
- **Google Search Console Domain properties want the bare hostname** (`templatebox.win`, no protocol) when creating the property, but want the **full absolute URL** (`https://templatebox.win/sitemap.xml`) when submitting a sitemap — the relative form (`sitemap.xml`) throws "Invalid sitemap address" on a Domain property because the host/protocol is ambiguous.
- **A freshly submitted sitemap shows "Couldn't fetch" instantly**, before Google has attempted any fetch — this is a UI placeholder, not a real failure. Don't chase it via Cloudflare Security Events unless the URL Inspection "Live Test" also fails; if Live Test passes, the sitemap status is cosmetic and will resolve on Google's own schedule (can take 1-2+ days). Forcing a re-check: delete and resubmit, or submit with a cache-busting query string (`sitemap.xml?v=2`) to force a treat-as-new fetch.
- **Cloudflare Security → Events** is the authoritative way to check whether Googlebot (or any specific requester) is being challenged/blocked — filter by path and/or user agent. A `curl` spoofing Googlebot's user-agent from a non-Google IP does NOT reproduce what Cloudflare's bot detection sees, since it checks IP/ASN and reverse DNS, not just the UA header — don't trust that kind of test as conclusive.
- **Adsterra's "Create Ad Unit" dialog only allows one placement per format-and-size per site** — it greys out exact duplicates. A second zone of a size you already have (e.g., a second distinct 300x250 Banner) requires a direct support ticket; be explicit in the ticket that you want a second zone for an *existing* ad slot's separate tracking, not additional ad inventory, or support may push back citing ad-density/CPM concerns. Different Banner sizes are NOT blocked, though: the 728x90, 320x50, and 160x600 blog zones were all created straight from the dashboard on July 18, 2026 with no ticket.
- **Adsterra's "Adult ads" toggle can reset to ON** on ad-unit-creation dialogs even after being turned off elsewhere for the site — recheck it each time before creating a new placement.
- **Ad script page resources always show as "couldn't be loaded" in Google's URL Inspection tool.** This is universal for any ad-monetized site (ad networks block crawler fetches of their own scripts) and is not a problem to fix.
- **Never bind an R2 bucket (or any Cloudflare product) to `templatebox.win` or `www`.** A custom domain binding takes over the entire hostname, so the website disappears — this took the site down on July 30, 2026. Asset hosting belongs on its own subdomain, e.g. `cdn.templatebox.win`. See `R2_CUSTOM_DOMAIN_ON_APEX_TAKES_DOWN_SITE.md`.
- **Verify domain routing by the header pair, never by the status code.** `Server: cloudflare` proves the proxy is in front; `x-nf-request-id` on the *same* response proves the request reached Netlify behind it. Either alone proves nothing — a 404 carrying `x-nf-request-id` is the site's own 404 page and healthy, whereas a Cloudflare-only response means the request never left Cloudflare. A bare 200 is not proof either, because R2 also returns 200 once objects exist.
- **Negative DNS caching outlasts a DNS fix, and local flushes do not reach upstream resolvers.** After the July 30 fix was correct at Cloudflare's authoritative nameservers and at 1.1.1.1/8.8.8.8/9.9.9.9, the local resolver kept returning NXDOMAIN and `Clear-DnsClientCache` did not help. Bypass DNS to test: `curl -D - --resolve templatebox.win:443:<edge-ip> https://templatebox.win/`. Treat a browser still showing an error as unproven, not as evidence the fix failed.
- **Netlify's "Pending DNS verification" plus a proxied record is a known deadlock.** Netlify resolves the hostname and sees Cloudflare's IPs instead of its own load balancer. Add or restore records grey-cloud (DNS only), wait for Netlify to verify and for the Let's Encrypt certificate to list the hostname, and only then switch to Proxied — then confirm SSL/TLS is Full (strict), because Flexible causes an infinite redirect loop against Netlify's forced HTTPS.
- **Two ignorable Cloudflare UI artifacts.** The DNS "Recommendations" panel ("Visitors cannot reach templatebox.win — add an A, AAAA or CNAME record") is cached and persists after the record exists; confirm against the record list instead. The apex-row tooltip about CNAME flattening is Cloudflare explaining that it makes an apex CNAME possible, not a warning.

## Design/Architecture Decisions Made During Build (with reasoning, in case revisited)

- **The fourth resume design shipped, and the Resumes cards stopped lying (August 30, 2026, later still).** Three of the four cards under Resumes carried **no `data-doc` at all**, so they launched the editor with no preset and rendered whichever template was last used: three cards, three distinct miniatures, one document -- and once Ruled Serif existed, non-deterministically so. `grey-rail` had been built and verified since August 2 and held back for one reason, that it reads `address`, `city`, `postcode` and `phoneAlt` which the form did not collect. The form collects them now (hidden for the three single-column templates, still collected), so it ships as **Modern Professional CV**, repointing that card rather than adding a fifth -- a fifth would have left the original three still promising designs they do not deliver. Four things a future session needs: **a palette entry may now name `accent`**, resolved one level deep in `colorOf`, which is how grey-rail's rail and display ink follow the swatch row without the descriptor saying `accent` at sixteen use sites -- before this the lookup returned the palette value verbatim and `"accent"` reached the painters as a literal string; **every template's `defaultAccent` must be a swatch on the row** (`applyAccent` matches the hex exactly, so grey-rail's `#4A4A4A` opened the editor with NOTHING selected until a graphite swatch was added) **and every swatch must stay dark enough for the rail's white sidebar text**; **`education[].field` is read by the descriptor and has no input**, which is safe not broken -- `buildRuns` drops an empty field with its dangling separator, so the head reads "MBA, 2012 - 2014"; and this is the site's **only two-column resume**, shipped design-led while the other three keep the unqualified ATS claim. Suite: only 2 parity differences, both resume @320, being the seventh swatch and the third picker button wrapping to new rows. Full write-up: `docs/implementation/FOURTH_RESUME_DESIGN_GREY_RAIL.md`.

- **`poster.html`'s jsPDF was blocked by a wrong SRI hash, and had been since `cc7acff` (found August 30, 2026).** It declared `sha512-yBpuTHfs...` where the file cdnjs actually serves hashes to `sha512-qZvrmS2e...`, so **every browser blocked the script** and `exportPDF()` in `js/poster.js`, which guards on `window.jspdf`, told the visitor "The PDF engine did not load. Check your connection and try again." -- blaming their network for a bad hash. PDF export on the poster editor was simply dead. The other four pages loading jsPDF all declare the correct **SHA-384**, so poster.html was given that identical string rather than a corrected SHA-512, and all five now match, which is what stops it drifting again. It surfaced as console noise while verifying an unrelated template; **the computed hash in the browser's own error message matched the real file**, which is what showed the fault was in the page rather than at the CDN -- worth remembering as the fastest way to tell those two apart.

- **Classic migrated onto the engine; js/resume.js now contains no renderer at all (August 30, 2026, later the same day).** Asked for as its own piece of work after the multi-page fix below exposed the reason: a Classic resume that exported to two pages said so NOWHERE, because its preview was a flowing HTML div with no concept of a page. **The equivalence check the engine document had been owing since August 2 was run first**, and run against the FILES rather than two instrumented code paths -- HEAD's `buildPdf` pulled verbatim out of `git show`, run in the same page as the engine on identical state, both outputs parsed straight out of their PDF content streams. Three cases (empty, one page, two pages): identical line counts, identical page counts, **0 text mismatches, 0 style mismatches, 0 horizontal differences**, and no line landing on a different page. Only vertical position moved, at most 11.3pt over two pages, and the cause is that the old writer's section spacing was incidental -- `line height + that block's trailing gap + 4mm`, so 23.7pt after a summary and 32.2pt after a description -- against one published 27pt. **The check earned its keep immediately: it caught `t.indent || 8` treating an explicit `indent: 0` as unset**, which had Classic's prose descriptions indented 8pt AND wrapped 8pt narrow. Nothing looked wrong; the diff reported `dx: 8` on exactly those lines. Then 237 lines were deleted -- `renderClassic` and its `el()` helper, `buildPdf` with `PAGE`/`INK_*`/`hexToRgb`, and the nine dead `.rs-*` CSS rules, confirmed unreferenced first. Four things a future session needs: **`classic` is a registry id now, not a sentinel** (saved documents naming it keep working because the descriptor carries that id); **`engineTemplate()` falls back to Classic rather than null**, and `selectTemplate()` takes the resolved descriptor's OWN id rather than testing truthiness, or an unknown id would be stored verbatim and re-resolved forever; **there is no longer any renderer that works without jsPDF**, so a `load` event arriving with no library now puts a message on the sheet instead of leaving an empty white pane; and the suite's HEAD-parity section reports the **preview pane getting SHORTER at 1024-1336px** (668.2px at 1024 rather than 788) because it is now exactly as tall as the A4 page it holds. Full write-up: `docs/implementation/CLASSIC_TEMPLATE_MIGRATION.md`.

- **The resume editor gained a template picker, and the descriptor engine draws the templates it offers (August 30, 2026).** A supplied Figma design -- a centred, fully ruled A4 CV in Times New Roman with green headings -- was asked for as an editable template under Resumes. It ships as `ruled-serif`, and delivering it meant taking `js/resume-engine.js` live: built on August 2 and verified in isolation, it had only ever been wired into `tools/resume-template-preview.html`, so no visitor had ever seen it. **Measurements came from the SVG**, not the Figma HTML export or the PDF -- it is the only supplied file carrying exact baselines on the 595.28x841.89 artboard, and the descriptor's numbers are the artwork's own rather than conversions.

  **Two renderers now exist on purpose.** `classic` is a **sentinel id, not a registry entry**: it names the hand-written preview and jsPDF writer that have always lived in `js/resume.js`, and they stay because they render every resume saved before templates existed. Migrating them owes the output-equivalence check the engine document asks for, which is separate work. The cost is bounded because **the two paths meet at exactly two places** -- `renderPreview()` and the download handler -- both branching on one expression, `engineTemplate(state.template)`, which returns null for Classic.

  **`engineTemplate()` exists rather than `TBResume.byId()`** because `byId` falls back to the FIRST registry entry for an unknown id. That is right for the harness and wrong here: a saved document naming a retired template would silently render as some other design. Null routes to Classic instead, so an unknown id degrades to the behaviour of an editor with no picker. The same guard covers a script that failed to load -- the picker then lists Classic alone.

  Four things that will bite a future session:
  - **`bottom` in a descriptor is a RESERVATION boundary, not the last baseline.** `ensureRoom` breaks the page when `baseline + lineHeight` passes it. Set to 815, just past the artwork's deepest baseline of 813.61, the document paginated; it is 830, which puts the deepest usable baseline at 813.2.
  - **`js/resume.js` is an end-of-body script and therefore runs BEFORE the deferred jsPDF tag**, which the engine needs in order to measure. The first engine paint on a cold load has nothing to measure with, so it is deferred to `DOMContentLoaded` (with `load` as backstop) and `.is-engine` is added only once a page has actually painted -- applied to an empty container it collapses the preview pane to nothing and back.
  - **The green is not hard-coded.** Every non-ink role resolves to `accent`, or the swatch row would be a dead control on this template -- the defect class this project has hit before. `defaultAccent` is applied when the template is CHOSEN and never when a saved document is restored, so a returning visitor keeps their colour. Classic declares none and therefore never imposes one.
  - **The three existing resume cards were deliberately left without `data-doc`.** `tests/verify-layout.js` keys each card by `data-doc || slug(title)`, so giving all three `data-doc="classic"` would collapse them to one id and orphan their three `CATALOG_ITEMS` entries. Accepted consequence: a visitor on Ruled Serif who clicks "Minimalist ATS Resume" stays on Ruled Serif, which is unchanged from how those cards have always behaved.

  **Two engine changes, both backward compatible and both fixing real problems.** `entries.sub` accepts an array, because this artwork stacks company, role and place on three baselines and the single line the engine allowed renders "Graphic Designer, Gaborone, Botswana". And one shared entry-emptiness gate, `entryHasContent()`, called by both the row loop and `sectionHasContent()`: the row loop previously skipped any entry whose head and bullets were empty, so an entry with a role and a location but no company vanished silently with the visitor's text in it. This does change `grey-rail` -- a row carrying only a company now renders -- which is the correct direction.

  One deliberate infidelity: the artwork sets `PROFESSIONAL SUMMARY` at 21pt and the other five headings at 24pt. That is the designer scaling the longest label by eye, not a rule -- it fits at 24pt with 260pt to spare -- so all six are 24pt. Full measurements, the landmark drift table and the ATS evidence: `docs/implementation/RULED_SERIF_CV_TEMPLATE.md`.

- **The receipts and invoices editor's bar carries the site mark alone at its far left, and Print and Clear Form beside Download (August 26, 2026).** Asked for in those words: the logo without the wording, at the far left end and ahead of the document name, and the two remaining actions on the navbar next to the download button. Both halves exposed a defect rather than being pure preference. **The house icon was rendering AFTER the name field**, at every width it was visible: `.editor-bar` publishes explicit `order` values and `.editor-home` is `order: 1`, while `.doc-name` carries no `order` at all and therefore sits at the flex default of 0 and sorts ahead of it. And **above 75rem there was no identity at the left edge at all** -- the shared rule hides `.editor-home` there so `mockup.html`'s `.editor-brand` wordmark can take the slot, and `docs.html` has no wordmark. What a future session needs to know: **`.editor-mark` is a new class and is deliberately ungated**, unlike `.editor-brand` which appears only from 75rem because `.editor-home` covers the band below it on the one page that has both -- here the mark REPLACES the icon, so a gate would leave a band served by neither, which is the shape of the dead search control one band over. **Its `order` is `-1`, not `1`**, because any positive value lands it after the unordered name input, and `-1` is the only value that fixes this bar without assigning an `order` to a class resume and poster share. **`.editor-bar .editor-mark ~ .editor-actions { margin-left: auto }` had to be put back**: `.site-header` is `justify-content: space-between`, the 75rem rule hands the free-space job to the search FIELD, and a bar with no field therefore split the space and floated the name 219px from the mark -- three classes so it beats the two-class rule inside the media query, since media queries carry no specificity. **Print and Clear Form moved, they were not copied** -- `js/docs.js` binds both by id, so a second copy would leave one of them permanently dead -- and every label in the bar including Download's is now wrapped in `.bar-label` and hidden below 48rem with the accessible name kept on an `aria-label`, because three labelled buttons do not fit a phone bar and the cost of overflow is a second row on a sticky workspace header. Measured: one row at 1440, 900, 768 and 390; 320px was already two rows before the change and is three pixels shorter after it. **`.preview-actions` stays, holding only the export note**, because it is what the ad anchor's lift is measured against and what the suite reads as `exportBar`; the dead `.doc-actions` rules were deleted, per this project's rule that retiring a unit deletes the rule sized for it. resume, poster and mockup are untouched. Full write-up: `docs/implementation/DOCS_EDITOR_BAR_MARK_AND_ACTIONS.md`.

- **The mockup editor is a workspace now, not a document with an editor on it (August 25, 2026).** Six changes in one session, written up in `docs/implementation/MOCKUP_WORKSPACE_REBUILD.md`. What a future session most needs to know: **`tshirt-model-white` declares `background: true` and `wood-a4` still must not**, and the two together are the whole eligibility contract -- the model's base is 1024x1536 with a transparent SURROUND (all four corners alpha 0, 39.3% of the image clear, because the figure was cut out of a studio backdrop) so a fill lands behind the scene, while the wood frame's transparency IS its print window so the same fill would land behind the artwork. No alpha test can tell those apart, which is why eligibility is declared. **The export panel's three sizes are derived from the canvas, never hardcoded**: full, half and quarter of 1000x1000 (drawn), 1024x1536 (model) or 2000x2000 (wood frame). The 1600x2000/800x1000/400x500 ladder in the reference cannot be used -- it would distort two of the three products and upscale the third. **JPG flattens onto white and the panel says so before the download**, because JPEG has no alpha and a straight write turns the whole transparent surround black. **The canvas is height-bounded through `--mockup-aspect`, not `object-fit: contain`** -- contain letterboxes inside the element box, and the pointer maths goes through `canvas.width / rect.width`, so every drag, resize and rotate would land off-pointer and the overlay canvas would drift. **`mockup.html` has no sticky `.preview-actions` any more**; the other three editors keep theirs and the anchor's lift rule for it is unchanged. **A My Mockups tile holds the STATE that produced it, not just the picture** -- colourway, custom hex, background and every layer's placement, with the bitmaps shared by reference and everything else copied in and out, so clicking a tile reopens an editable mockup and editing it does not rewrite the entry it came from (share the object and the tab silently becomes several copies of the current render; the suite asserts against exactly that). Pressing Add after editing a reopened mockup saves a NEW entry -- entries are versions, there is no in-place update. **The two preview panels need `.mockup-stage[hidden], .mockup-tray[hidden] { display: none }`** -- an author `display` beats the UA sheet's `[hidden]` rule, and without it the live canvas rendered above the tiles for a full day with the My Mockups tab open, reported by screenshot because the suite was asserting `element.hidden` (the property, true the moment the attribute is set) instead of computed display. Same trap as `admin.html`'s catalog preview. Panel checks measure rendering now. A tile takes the RENDER's shape rather than a forced square -- `aspect-ratio: 1/1` with `object-fit: contain` letterboxed the model photograph, measured at 84x127 inside a 127x127 tile with a third of it empty -- and the check for that has to run on the model template, since a square tile and a correct one are indistinguishable on a 1000x1000 drawn product. The loaded tile is marked in the accessibility tree with `aria-current="true"` as well as visually, and only on that tile; the suite checks it at two points in the sequence, because set-once-never-cleared and cleared-but-never-set both look right at a single moment. Still session-scoped, since the bitmaps are never written to storage. **Transparent moved into the background picker's preset grid as its first button** (`allowClear` on the factory, background instance only) when that panel's swatch row became a hue strip -- without the move, choosing a background would have been a one-way door out of the editor's default state.

- **The mockup editor's Colour panel lost its colorway swatch row, and the panel lost three standing hints (August 25, 2026).** Asked for as: remove these unnecessary paragraphs, and make the colour section a strip like the reference image instead of swatch blocks taking space, because the colours are already fine in the dropdown. Both halves are about the same scarce resource -- the control column is 20rem wide beside the stage and stacks ABOVE the preview on a phone, so every removed band is scroll a visitor pays before reaching the canvas. Measured by the suite's own snapshot: the control pane is 233.5px shorter at 1920px and 351.9px shorter at 320px. What a future session needs to know: **the inline strip is not a lookalike of the popover's hue track, it is that track a second time** -- `createColorPicker` holds a `hueTracks` list now, one `hue` per instance, one handler bound across both, both thumbs painted by the same `sync()`, and the background picker passes no inline nodes so its list has one entry and it behaves exactly as before. **The Background panel's `.color-row` and its hint were deliberately kept**: its first quick pick is Transparent, a real choosable state that no hex in the popover can express, and its hint describes the exported file rather than anything visible on screen -- that asymmetry between the two panels is a decision, not an oversight. **The colorways lost their buttons, not their effect** -- a product still opens on its default colorway, which is where the drawn garments get their hand-picked outline colour; anything chosen after that derives its outline by darkening, as a picker-chosen colour always did. The strip is pointer-only and `aria-hidden`, the same as the popover's track and for the same reason (the trigger opens focusable Hex/R/G/B fields and preset buttons setting the identical value), so no colour became keyboard-unreachable -- the shortcut to four of them did. **This supersedes one clause of the August 24 background-colour entry below**, which says the swatch row stays outside the factory: there is no swatch row on the Colour panel any more, and the canvas label is now the only thing left outside it. **Testing note:** the suite's colourway check drove `#m-color-row .swatch` and had to be rewritten -- it presses the strip with **trusted input**, because `bindTrack` calls `setPointerCapture`, which throws on a pointerId with no live pointer, so a synthetic `PointerEvent` reports a dead strip against working code. It asserts the garment pixel, the trigger's hex label and **this** strip's thumb position all agree, the last of which is what catches a strip wired to the colour but not to `sync()`; confirmed failing against a deliberate break. Full write-up: `docs/implementation/MOCKUP_CONTROL_PANEL_TRIM.md`.

- **The header search control is a link to a page, not a reveal (August 24, 2026).** It was a button toggling a `search-open` class on `.site-header`, and it did nothing at all from 361px to 992px: the one rule that undid the field's `display: none` was written inside `@media (max-width: 22.5rem)` while the `display: none` was scoped to 62rem. It worked at 360px -- the width anyone testing a phone reaches for first -- and nowhere else. Repairing the reveal was rejected: a 320px header has no room for a field, and an in-header field can only filter what is already on the page, so it can never reach the guides. `site/search.html` is the answer instead. Both dead rules were deleted along with `setSearch`, per this project's own rule that retiring a unit deletes the rule sized for it. The inline field above 62rem is untouched -- it filters the catalog in place, which is right on the page that IS the catalog, and it is why the link stays hidden there. See `docs/error-fixes/HEADER_SEARCH_BUTTON_DEAD_ON_PHONES_AND_TABLETS.md`.

- **`search.html` imports the real homepage cards rather than duplicating the catalog (August 24, 2026).** It fetches `index.html`, parses it with `DOMParser` and `importNode`s the actual `<article class="template-card">` elements. A `js/catalog-data.js` registry was the obvious alternative and was rejected: the eighteen cards are hand-authored markup carrying CSS document miniatures, photo thumbnails and the launch attributes, so a registry would be a THIRD list to keep in step with `index.html` and with `CATALOG_ITEMS` in `js/admin.js` -- two lists the suite already has to cross-check because they drifted -- and it still could not reproduce the miniatures. The cost, stated plainly: the search page depends on `index.html`'s markup shape at runtime and would render nothing rather than throw if `.catalog-grid .template-card` or `.card-body` were renamed. Both halves are covered -- the hook scan now reads `js/search.js`, and section 3c clicks a card in both of the page's states. **The trap worth knowing: cards inserted after `initCatalog` are unbound and go straight to the editor, skipping `loading.html`.** `js/app.js` exports `bindLaunchControls` for this. A guard written `window.TB && ...` is always false, because `TB` is a top-level `const` and therefore a lexical global rather than a window property -- that exact mistake sent a result card to `docs.html` during the build.

- **The mockup editor's background colour is declared per template, never inferred (August 24, 2026).** The four drawn products are always eligible: they paint onto a cleared canvas, so everything around the garment is transparent and exports that way. A photographic template qualifies only by `background: true` in `js/mockup-templates.js`, and neither shipped template sets it. Detecting eligibility from the base image's alpha channel is the wrong answer and is specifically wrong for the template most likely to be tested against: `wood-a4`'s base IS transparent inside its print window, because that transparency is the mask the artwork shows through, so an alpha test would qualify it and paint the chosen colour behind the poster. Distinct from `backing`, the white paper sheet behind artwork inside a frame's window, which is unchanged. Transparent is the default and an export is byte-identical to before unless a colour is chosen. The fill goes on the canvas before everything else, which puts it in the PNG export and the tray thumbnails for free; CSS would put it in neither. The colour picker became a `createColorPicker` factory with two instances -- the swatch row and the canvas label stay outside it, because the two pickers hold different state and generalising those would need more injection points than shared code.

- **The mockup editor's bar switches on ONE boundary, 75rem (August 24, 2026).** Above it: wordmark, a Mockups dropdown, Templates and Guides, a search field. Below it: the all-templates icon, a hamburger holding the same links, a search button. A second boundary was available (the site header's own field is gated at 62rem) and was rejected -- it would leave a band half collapsed and, worse, put the CSS and the JS into disagreement about when the bar IS collapsed, which is the shape of the dead search control one band over. The dropdown items are plain anchors with `data-target`/`data-doc`, so `bindLaunchControls` routes them through `loading.html` with the preset and no routing code is written for them; the accepted consequence is that switching mockups from inside the editor costs the interstitial, as every other launch does. `#doc-name` is gone at every width -- `#m-label` in the controls names a mockup now -- and the download button's label is hidden visually on phones (kept as its accessible name) because with it the bar wrapped and the sticky header went from 85px to 141px on a workspace. **`mockup.html` only**: `.editor-brand` is a new class and `.editor-home`, `.doc-name` and `.dl-toggle` are untouched, so resume, docs and poster come out byte-identical against HEAD at all fourteen widths.

- **Catalog thumbnails are reshaped to the card's ratio at intake, and `contain` stays (August 24, 2026).** The Leaning Wood Frame card left 73.6px of a 358px card empty because its thumbnails were 1000x1000 in a 4:5 window. `object-fit: contain` was NOT changed to `cover`: it was chosen deliberately on August 23 because a design carries content to its edges, and `cover` would silently crop exactly those edges on every future upload. The file is made the card's shape instead, after which the two keywords are identical and the CSS decides nothing. `admin.html`'s intake now offers Fill (centre-crop, default, right for a scene) and Fit (pad, right for a design with a printed border), and **Fit pads with transparency rather than a colour** -- a baked light pad is a pale band on every dark-theme card, permanently, because a file cannot answer a media query. The `alreadyFits` fast path tests shape as well as size now: without that, a small square upload is kept byte for byte and reaches disk untouched, which is how this happened. Three files were re-run through that same intake and `index.html`'s `width`/`height` updated to match, because those attributes reserve the box and changing the file without them trades a visible gap for a layout shift. **The August 23 stylesheet comment claiming every shipped thumbnail was already 4:5 was wrong when written** -- two were not -- and is corrected in place, because "this changes nothing today" is a claim worth checking against the files.

- **Catalog thumbnails are managed in `admin.html`, and the panel exports artifacts rather than driving a catalog registry (August 22, 2026).** Asked for as: let me attach my own thumbnails to catalog items, one for the default view and, if I want it, a second for the hover effect. The two-image convention it targets already existed on two of the eighteen cards (`card-thumb-blank` at rest, `card-thumb-hover` crossfading in on hover and focus); what is new is being able to attach that pair to any card without hand-editing `index.html`. **The hover image is optional and a card with only a default is complete** -- sixteen of the eighteen are exactly that today, drawing CSS miniatures instead of photographs -- so it never blocks a save. Full write-up: `docs/implementation/CATALOG_THUMBNAIL_ADMIN.md`. What a future session most needs to know:
  - **The obvious alternative, a catalog registry along the lines of `js/mockup-templates.js`, was rejected and it will look like an oversight later.** Three reasons, in order of weight. There is no catalog data file to edit: the eighteen cards are hand-written markup, so a registry means rewriting the feed to render from JavaScript, and **the card titles are the only crawlable internal links to the editor pages** (`SEO_AUDIT.md` finding 2.1) -- moving them into a runtime render is precisely the defect `BLOG_POSTS_NOT_CRAWLABLE_WITHOUT_JAVASCRIPT.md` records for the blog. A browser page cannot write into `site/assets/` with no server and no build step, so images must be downloaded and placed by hand whatever the data model is; the registry would have moved the markup step, not removed the file step. And the blog panel already establishes edit-locally-export-place-commit, so a second workflow in the same tool behaving differently would be the surprising thing. **The accepted cost is that the item list is hardcoded in `js/admin.js` and the two are maintained by hand** -- the same silent-drift class as `EXPORTED_POST_PAGE_FOOTER_DRIFT.md`. **Six checks now guard it** (August 23, 2026, section 1j of `tests/verify-layout.js`): the picker must list exactly the cards `index.html` ships, with matching title, category and `data-doc`; each `CATEGORIES` record must match the cards it describes; and the catalog-empty message must state the real count. Mutation tested with ten deliberate breaks, all ten caught, every check firing on at least one. Drift is still possible -- nothing generates either list -- but it can no longer ship unnoticed. **Worth knowing for the next mutation test: the repository's files are CRLF, and three of the ten mutations silently matched nothing until that was handled. A mutation that does not apply is indistinguishable from a check that passed.**
  - **The preview is the feed's own markup, not a copy of it.** A real `.catalog-grid` > `.template-card` > `.card-preview.photo` tree, so the 4:5 window, the hover crossfade, the card lift and the rounded geometry are all inherited and cannot disagree with what ships. Two `[hidden]` overrides were needed because an author `display` beats the UA stylesheet's `[hidden] { display: none }`: `.catalog-grid` sets `display: grid` on the wrapper and `.card-preview.photo .card-thumb` sets `display: block` on the images -- without the second, the empty hover slot paints over the default image, which is the optional case and therefore the common one. The single-column override must stay declared **after** `.catalog-grid`'s column ladder for the usual reason: media queries carry no specificity.
  - **Blog publishing writes all four artifacts itself, and delete finally reconciles (August 24, 2026).** A post needed four things placed by hand -- `js/blog-data.js`, a page per post in `blog/`, the guides archive pasted into `blog.html`, and a URL added to `sitemap.xml` -- and forgetting the third leaves the post invisible to anything that does not run JavaScript. Publish to Project now does all four through the same `TBProjectFolder` the thumbnails use. **Two are whole-file writes** (the data file and every post page are generated in full), so only `blog.html` and `sitemap.xml` need verified splices. **The sitemap patch touches only `<url>` blocks whose `<loc>` is a blog post** -- regenerating the file from the post list would be shorter and would silently drop the editors, the landing pages and the homepage, so verification refuses the write if any non-post URL disappears. **Delete and hide now reconcile**: a removed post previously left its page on disk and its sitemap entry live, still served and still crawlable. Same ordering discipline as the thumbnails -- write and patch everything, reconcile LAST -- because deleting first would leave both patched files pointing at pages that no longer exist. `admin-fs.js` broadcasts connection changes so one folder grant covers both panels.
  - **Chrome caches filesystem state on a directory handle, and a publish invalidates its own (August 25, 2026).** Reported as `Stopped: An operation that depends on state cached in an interface object was made but the state had changed since it was read from disk.` Both publish routines write into a directory and then resolve more paths through the same long-lived handle, which is exactly the pattern that raises `InvalidStateError`. **It cannot reproduce against an origin-private directory, which is all the test harness can reach** -- so the code had been tested end to end against a filesystem incapable of the failure, and the defect reached the owner instead. Every operation in `js/admin-fs.js` is now wrapped in a one-shot retry that re-resolves from the root; one retry rather than a loop, because a second failure is a real problem worth surfacing. Also fixed: `looksLikePublishDir` swallowed every exception and so reported a transient stale handle as "that folder is not the site directory", sending the operator to re-pick the folder they already had right. Verified by injecting the exact DOMException mid-publish on both paths. See `docs/error-fixes/PUBLISH_FAILED_ON_A_STALE_DIRECTORY_HANDLE.md`.
  - **Blog cover uploads are compressed to fit instead of refused, and the encoder became a shared module (August 24, 2026).** The blog half still hard-rejected anything over 400 KB -- "Rejected: image exceeds 400 KB" -- while the catalog half of the same panel had been auto-compressing since the day before, so an ordinary phone photograph was unusable as a cover and the operator had to go and shrink it by hand. **The pipeline moved to `js/admin-image.js` rather than being copied**, because both halves need identical mechanism and differ only in numbers, and a second encoder would drift exactly the way the footer constant and the inline route whitelist already have -- invisibly, with both halves still working. `TBAdminImage.prepare(file, options)` takes the budget, the pixel cap and an optional target ratio; the catalog asks for 60 KB at 1000px reshaped to 4:5, covers for 120 KB at 1600px with no reshape (a cover has no fixed frame). **The cover budget is deliberately far tighter than the pixel count suggests**: a cover is inlined into `js/blog-data.js` as a data URI and base64 adds a third on top, so every cover is downloaded by every visitor to `blog.html`, not only the one reading that post -- ten posts at the old cap would have been a 5 MB script. Measured: an 11.3 MB 3000x1800 PNG comes out 111 KB WebP at 1600x960 with its aspect ratio intact, and an in-budget 79 KB upload is still kept byte for byte. The catalog path was re-verified unchanged through the module (1600x900 in, 540x675 4:5 out) and the suite's section 6 covers it.
  - **The thumbnail workspace reads the card before offering to replace it (August 24, 2026).** It was write-only: a card that ships two thumbnails presented an empty form, so uploading a replacement default produced a record with `hoverImage: null`, and every downstream step honoured that faithfully -- single-image markup, hover `<img>` dropped from index.html, hover file deleted by the cleanup. Each step was individually correct; the record simply described an intention nobody expressed, because nothing in the panel had ever read what the card already had. **On the mockup cards that destroyed the effect the card exists for** (bare product at rest, styled demo on hover) on a fully SUCCESSFUL publish -- so the earlier deletion-ordering fix, which only protects a failed one, did not touch it. Now `hydrateFromProject` reads index.html, locates the card with the same locator the patcher uses, loads its current thumbnails off disk and fills the form, taking the destination folder from the real `src` path. A saved workspace record still beats hydration on purpose, so unsaved work is never overwritten by a background read, and a stale-token guard stops a slow read landing in another card's form. Second line of defence for when hydration cannot run (no folder connected, or a workspace saved before this existed): publishing REFUSES when the live card has a hover thumbnail, the record has none and Remove Hover Image was never pressed. `hoverCleared` is set only by that button and cleared by a replacement upload -- it is what separates "this card should have none" from "one was never loaded", which are otherwise the same absent value. See `docs/error-fixes/PUBLISHING_ONE_THUMBNAIL_DELETED_THE_OTHER.md`.
  - **Publish writes into the working copy directly, via the File System Access API (August 23, 2026).** Asked for as: why is all of this manual, why does Save not just do it. The answer is that a page cannot reach the file system on its own -- a browser security rule, not a gap here -- and this API is the sanctioned exception: the operator picks a folder once in a native dialog this code cannot script, and the grant covers that folder alone. `js/admin-fs.js` is the wrapper (handle, permission states, IndexedDB persistence, read/write/delete/list); the catalog-specific publish routine stays in `js/admin.js`. **Chromium only** -- Firefox and Safari ship no `showDirectoryPicker`, so the Publish controls never appear there and Download plus Copy Markup remain the whole workflow. Deploying is still manual in every path, deliberately: no page can push to a repository on the owner's behalf. Four things not to undo. **The edit to `index.html` is a byte splice of one element, never a DOMParser round-trip** -- reserializing would normalize whitespace, entities and void tags across all 47 KB and turn a two-line change into an unreviewable diff; measured, replacing a card's preview moves 12 lines and nothing else. **Comment bodies are blanked to equal-length spaces before any scanning**, so offsets stay valid while a commented-out card cannot steer the splice (verified: a commented copy is ignored and the real card is patched). **Images are written before the markup**, because a half-failed run then leaves an inert unreferenced file rather than a live homepage pointing at a file that was never written. And **nothing is written until `verifyPatch` passes**: the file must not have lost a quarter of its length, the markup must parse, the card count must match, every other card's category/link/variant/title must be unchanged, and the new image paths must appear. Tested against a real copy of `index.html` with the picker stubbed to an origin-private directory handle, so the whole path ran for real: eleven cases including duplicate cards, an unbalanced preview block, a missing preview, a commented-out card, a wrong folder, and a splice that swallowed four cards -- the last caught by `verifyPatch` with "card count became 14, expected 18", file unchanged.
  - **The wood-frame hover composite on the poster cards was removed (August 23, 2026).** The three `.card-preview.framed` cards ran a three-layer effect: at rest a drawn poster, on hover the drawn border and caption dropped away while the plate slid into the print window of a photographed leaning wood frame that faded in over it. Earlier the same day it had been made to survive a published thumbnail -- the hover rule positions ONE box into the window, so an `<img>` served as well as a `<div>`, and `CATALOG_ITEMS` carried a `framed: true` flag the generator, preview and publish routine all respected. **The owner then asked for the effect gone, and all of it went with it**: both `@media (hover: hover)` blocks keyed to `.card-preview.framed` (about 5.6 KB of `css/style.css`), every `framed` branch in `js/admin.js`, the `is-unavailable` field styling, and the disabled hover upload. `Framed Photo Poster` is `card-preview photo` with its real thumbnail; the other two keep their drawn miniature under a plain `card-preview`; all three take a second hover thumbnail again like any other card. **`assets/mockups/wood-a4-base.png` and `wood-a4-overlay.png` stay** -- they are the mockup editor's own template assets, registered in `js/mockup-templates.js`; only the homepage's use of them as a hover background is gone. **The `contain` argument survived the removal, and is now site-wide**: rather than special-case the one card, `.card-preview.photo .card-thumb` moved from `object-fit: cover` to `contain`, so no catalog thumbnail is ever cropped. It had to be the SHARED rule -- a bespoke class on that card would be dropped the next time it was published from admin.html, whose generator emits a fixed `card-preview photo` block, which is the same silent loss the `framed` machinery existed to prevent. It changes nothing on disk: every shipped thumbnail is exactly 4:5 (800x1000, 600x750) where the two keywords are identical, verified by measuring all five images after the change (mockups still draw the full 285x356 box; the 707x1000 poster draws 252x356 with 33px of ground either side and zero vertical crop). The mat is the card background token, so it follows the theme.
  - **Uploads are compressed to fit, not rejected for being too big (August 23, 2026).** The original 500 KB cap rejected an ordinary phone photograph, which made the panel unusable for its actual purpose. Uploads are now resized to a 1000px long edge and re-encoded to 60 KB or less in a canvas, with no library and nothing leaving the browser. Calibrated against the four thumbnails already on disk (46-83 KB at 600x750 and 800x1000, about 0.8 bits per pixel). **This is lossy and cannot not be** -- that is what takes a multi-megabyte PNG to tens of kilobytes -- but measured against a lossless downscale of the same source it comes back at 45.3 dB PSNR at the encoded size and 49.4 dB at the size a card actually renders, where above 40 dB is the conventional visually-indistinguishable threshold. **An image that already fits is kept byte for byte and never re-encoded**, so re-saving does not degrade a thumbnail a generation at a time. Four things not to undo: downscaling happens in halving steps because a one-shot `drawImage` undersamples badly on a large reduction and aliases fine detail; WebP is preferred because it reaches the same appearance in a third fewer bytes, which is the only reason 60 KB is achievable at these dimensions, and support is detected by encoding a 1x1 canvas and checking the blob's TYPE (a browser that cannot write WebP silently returns PNG rather than failing); transparency picks the format list and every pixel is scanned rather than sampled, because a sampled scan misses a one-pixel transparent border and JPEG would return it as a black outline; and quality is binary-searched for the highest that fits rather than fixed, giving up resolution only when quality alone cannot get there. **Consequence worth knowing: new thumbnails are WebP while the two on disk are JPEG, so a re-export writes beside the old file rather than over it and orphans it.** The download status says so every time, because nothing in a browser can see the repository to check.
  - **The file extension comes from the mime type, never from `file.name`.** The name is user-controlled; a file called `art.jpg` that is really a PNG would download as `.jpg` and the generated markup would point at a file the deploy does not contain. Same reasoning as the mime gate itself, one step further on. Intrinsic dimensions come from a decode probe and go into the markup's `width`/`height`, without which the feed shifts as thumbnails load.
  - **Markup elements are built in an inert `document.implementation.createHTMLDocument("")`.** An `<img>` created in the live document and given a `src` fetches it even while detached, and the `src` is by definition a file that does not exist yet -- so every Copy Markup logged a 404 for the thumbnail it was describing. Found in the browser, not in review. The opening tag is taken from the serializer rather than by slicing to the first `>`: nothing in the HTML serialization rules escapes `>` inside an attribute value.
  - **Found in passing: `index.html`'s catalog-empty message said "all 17" against eighteen cards.** The count was not updated when `tshirt-model-white` was added. Corrected to 18. A new card added through this panel needs the same edit, and the panel's own copy-confirmation says so.
- **The ad rail is a fixed full-height column and the page is inset to its left (August 10, 2026).** `position: fixed; top: 0; right: 0; bottom: 0` at the band's width, on `index.html` and all four editors. The column owns the window's right edge from y=0 to the foot of the viewport; the menu bar ends where the column begins instead of running underneath it. This is the Photopea layout, asked for by name. **It reverses two things this document said the day before**: that the rail is sticky at `top: 5.75rem` deliberately clearing the header, and that a fixed full-height column with body padding "was tried and reverted". The owner reinstated it knowingly, to have the unit in full display, and asked for this entry so the next session does not revert it a second time. **Do not restore the sticky version.** Full write-up with every measurement: `docs/implementation/FIXED_FULL_HEIGHT_AD_RAIL.md`. The parts most likely to be second-guessed:
  - **The inset is ONE mechanism: a single `padding-right` on `body`**, applied by `.has-ad-rail`, which `js/ads.js` adds only once `mountPlacement` has actually filled a banner. Everything in normal flow inherits it, which is the entire reason `.site-header` needs no rule of its own -- it is `position: sticky`, so it stays in flow and its background stops cleanly at the column's edge. The mega-menu, the sticky category tabs and the editors' sticky export bar are all inside that padding for the same reason. **If a width or margin hack on the header ever looks necessary, the padding is in the wrong place.** The two `position: fixed` anchors are the only things that do not inherit it, and they mount only under 48rem where no rail exists -- checked, not assumed.
  - **The page width caps had to shed the reserved width, not reserve it twice.** `main:has(.editor-shell)` was raised to 84rem/93rem and `main:has(.home-layout)` to 96rem precisely so the old *in-flow* rail was added beside the content rather than taken out of it. Those tiers are still there, because with the script blocked they describe the layout that must not change; `body.has-ad-rail` overrides them (72.75rem for editors, `calc(96rem - var(--ad-rail-w))` for the homepage). Editing panes measure 546px at 1920px and 546px at 1440px, against 546px and 544px before -- no pane is narrower anywhere. The homepage feed comes out a uniform 8px wider, which is the gap/gutter difference. **The homepage's 96rem cap and its rail landed in the same commit, so the cap always had the rail inside it** -- that is why leaving it alone would have grown the cards by a third on a wide screen rather than leaving them alone.
  - **`max-height: calc(100vh - 7rem)` is gone, not merely retuned.** `top: 0` with `bottom: 0` pins the height to the viewport, and the 7rem it used to shed was the header clearance a column starting at y=0 does not need. The internal scroll, the hidden scrollbars and the `:empty` slot collapse all stay.
  - **Verified against a pristine `git archive HEAD` copy served side by side**: with `js/ads.js` blocked, 455 measured properties across five pages and seven widths came back with zero differences. That is the check that matters most here, because the whole placement is supposed to be invisible when it does not fill.
- **Homepage feed reworked: denser ladder, cards as links, hover previews, no descriptions (August 10, 2026).** Five changes at the owner's request, confined to `index.html` and `css/style.css` with no JavaScript touched. Full write-up and every measurement: `docs/implementation/HOMEPAGE_FEED_REWORK.md`. What a future session most needs to know:
  - **Feed previews fill their tile edge to edge.** `.card-preview` used to carry a `--space-md` inset, so each CSS miniature floated inside its tile with its own border reading as a second rectangle inside the card's border, while the one photographic card filled its tile completely. The padding and the miniature's border are gone for feed cards only. **Scoped to `.catalog-grid` on purpose** -- the nine `*-template.html` landing pages use `.card-preview` for a hero preview where the inset is right. The poster miniature keeps its border because there the border is the drawn picture frame, and the `:not(.poster)` guard is load-bearing: without it that selector ties `.mock-doc.poster` on specificity and source order alone would delete the frame.
  - **Dark mode is site-wide, and the palette is a value swap rather than a rewrite (August 11, 2026).** Every colour is written once as an `--l-*` / `--d-*` pair in `:root`; nothing reads those directly, every rule reads a `--color-*` alias, and switching theme only re-points the aliases. A colour change is a one-line edit in one place. Full write-up with measured contrast ratios: `docs/implementation/DARK_MODE.md`. What a future session most needs to know:
  - **The claim that the stylesheet was fully tokenized was FALSE, and it was worth checking.** The audit found 45 hardcoded colour values outside `:root`. Most were fine to leave (product and artwork colours are content, like photographs), but `.save-state` and `.form-error` had to be tokenized because a forest green and a deep red are unreadable on a dark ground. **Re-audit before assuming the stylesheet is clean again** -- `grep` for hex outside `:root` is a one-line check.
  - **Document sheets never go dark, and this is deliberate.** `.resume-sheet`, `.doc-sheet` and the feed's `.mock-doc` miniatures depict printed paper. Their grounds were already hardcoded light, but their DESCENDANTS read tokens -- labels, table headers, the navy and forest document accents -- so a blind swap would have put `#1F4E79` headings on white paper. The fix re-declares the light palette at the top of each paper surface, so every descendant resolves light without any component rule knowing a theme exists. **`--accent` is untouched by the theme for the same reason**: it is ink on those sheets, and all ten of its use sites are inside them.
  - **Print is not themed.** The print block re-points the aliases back to light before anything else. Without it, printing a receipt in dark mode sends a near-white `--color-text` onto white paper and produces a nearly blank page. This is asserted by a test, not just written down.
  - **The no-flash script is inline and synchronous by necessity**, in the `<head>` of all 24 themed pages before the stylesheet link. An external file loads after first paint, which is the flash it exists to prevent. **The toggle always writes an explicit `data-theme` value and never removes the attribute** -- an absent attribute means "follow the OS", so removing it would overrule a visitor who chose light on a dark-set machine.
  - **Two duplications are guarded by tests rather than trusted.** The dark palette is declared twice (explicit attribute, plus a `prefers-color-scheme` fallback for visitors without JavaScript) because CSS cannot share a declaration block between them, and `tb_theme` is spelled in `js/app.js` and in 24 inline snippets. `tests/verify-layout.js` asserts both stay in sync, and all four new checks were mutation-tested.
  - **`tools/og-image.html` gets the theme but no toggle**, because it has a header and no `js/app.js`. An unwired control is worse than none.
  - **Known and accepted:** an unfilled ad iframe is a bright white rectangle in dark mode. The containers adapt correctly; the creative is a cross-origin document whose own canvas paints white and nothing on our side can style it.
- **The mega-menu panel now anchors to the header, not to the More button (August 11, 2026).** Adding the theme toggle to `.site-nav` pushed the panel 5px off the left edge at 1200px, and it had only 3px of margin at 1024px even before that -- its left edge depended on how many controls sat to its right. `.nav-more` is no longer a positioning context; the panel anchors to `.site-header` (position: sticky, therefore a containing block), inset by the header gutter and clamped with `max-width`. **`min-width: 52rem` became `width: 52rem` because min-width beats max-width when they conflict**, so a min-width panel cannot be clamped at all. Left edge is now 145px at 1200 and 153px at 1024, and independent of what else sits in the nav.
- **Catalogue cards are ROUNDED, and that contradicts the theme on purpose (August 10, 2026).** `--radius-card: 0.75rem` on `.catalog-grid .card-preview`, at the owner's instruction, for the Pinterest-style look. **This is the first rounded geometry on the site.** `css/style.css`'s own header said "Geometry: Flat, sharp, rectangular", `PRD.md` line 31 said the same, and `MOCKUP_GENERATOR_IMPLEMENTATION.md` quoted the header to justify the flat-vector product illustrations -- all three now record the exception and its scope, because stale documentation is what the next session trusts over the CSS. CLAUDE.md carries no geometry claim (checked, not assumed). The scope is deliberately narrow: everything outside `.catalog-grid` keeps square corners, and no gradients or drop shadows were introduced anywhere. **If a second rounded surface ever appears, the exception has become drift** and the wording should be rewritten rather than extended. The rounding is load-bearing with `overflow: hidden` on the same element -- that is what clips the hover zoom and both crossfades inside the curve instead of flashing square corners under a rounded mask.
- **The 5-column band is gone; the column count was the culprit, not the gap (August 10, 2026).** Reported as too much empty space between columns. Measured before changing anything: at 1920px the four gaps came to 96px of a 1164px feed, 8.2%. Deleting the gap ENTIRELY would have taken a card from 213.6px to 232.8px, +9%; dropping the fifth column takes it to 279px, **+31%**. So the fifth column went and the gap came down as a second-order improvement (1.5rem to 1rem desktop, 1rem to 0.75rem mobile). Ladder is now 4 at 64rem, 3 at 48rem, 2 below. **The next lever, if the cards still read small, is the 48-64rem band** -- 3 columns give 229.7px at 768px and two would give 352.5px, which is a large card for a tablet and worth looking at rather than assuming.
- **The category tabs and the header hide on scroll-down at every width (August 10, 2026; mobile-only from August 13; every width again from August 14 -- see the entry below before trusting the "mobile-only" wording anywhere else it may still appear).** One utility in `js/app.js`, `initScrollDirection()`, which knows nothing about any element: it writes a single `.is-nav-hidden` class onto `<body>` and CSS decides who reacts. Passive listener, one rAF per frame, a 6px jitter floor and an 80px reveal zone at the top of the document. Four things worth keeping:
  - **The hidden state is a transform, never `display: none`.** Both elements are `position: sticky`, and collapsing them would drop their boxes out of flow -- **index.html's ad-rail inset works precisely because `.site-header` stays in normal flow and inherits the body padding**. A transform moves the paint, not the box.
  - **The tabs travel their own height PLUS their sticky offset.** `-100%` alone would park them on top of the header and leave them visible under a hidden header. Measured hidden bottom edge: exactly 0px at every width, on both the desktop and mobile offsets (see below).
  - **Scoped to the homepage** with `body:has(.home-layout)`. The class is written on every page loading `js/app.js`, but the editors do not opt in: they carry their own sticky export bar and fixed ad anchor, and a header moving under those is untested. `:focus-within` un-hides both, so keyboard focus can never be stranded off-screen -- those rules tie on specificity and win on source order, so they must stay declared last.
- **August 14, 2026: the header hides at every width again, not only below 48rem, reversing the August 13 entry directly below.** Asked for explicitly ("do the same for all devices"): the mobile-only behaviour read as more polished than the always-visible desktop header, and the inconsistency between the two was the complaint. The August 13 reasoning -- that filtering is a primary action and the tabs should stay reachable without vanishing with the surrounding chrome -- is not overturned by this, because **the tabs still never fully hide**; they only rise to close the gap the header's retraction leaves, exactly as they already did on mobile, and that rise now happens at every width too. `.feed-tabs` sticks at `top: 5.25rem` on desktop and `top: 4.75rem` below 48rem; the header-close translateY uses whichever of those matches the current breakpoint. **Both offsets were checked against the header's real rendered height rather than assumed**, because the header uses `flex-wrap` and carries a search box that can wrap it onto more than one line: measured, the header holds a stable 85px (5.313rem) at every width from 769px to 1920px, 1px off the 84px the desktop offset reserves. Below 48rem the header can exceed 200px on the narrowest phones (search and nav stacking under the wordmark) and the 76px reserved there already undershoots -- that mismatch predates this change and is not fixed by it. Verified with trusted CDP wheel-scroll input (not `window.scrollTo()` from injected JS, which was found not to reliably fire the native `scroll` event the page's listener depends on) at 14 widths from 320px to 1920px: header hides and returns, tabs rise and return, tabs land flush with the viewport top (0px gap) in every case, keyboard focus still rescues a hidden header on desktop, and the editors/about/etc. remain correctly un-opted-in. Full site suite (`node tests/verify-layout.js`): 880 passed, 0 failed.
- **The category tabs stopped hiding entirely and the header hide narrowed to mobile only (August 13, 2026) -- superseded the next day, see above.** The reasoning stands on its own terms even though it no longer governs: filtering is a primary action on this page and should stay reachable through a scroll rather than disappearing with the chrome around it, so only `.site-header` responded to `.is-nav-hidden`, and only below 48rem.
- **The feed cards zoom on hover (August 10, 2026).** A new `.card-media` wrapper inside each `.card-preview` carries `transform: scale(1.06)`. **The wrapper is not decoration**: the poster cards composite a frame photograph over artwork positioned inside the frame's window, and scaling those separately -- each about its own centre -- slides the artwork out of the opening. One wrapper means one transform-origin and the composite stays registered, and it is also why the zoom cannot desync from either hover crossfade. The clip stays on `.card-preview` and the scale on the child, per the rule that scaling a clipping box scales the window too. Transform only, so it cannot repack the grid; gated behind `@media (hover: hover)`; reduced motion is covered by the global block, which drops the animation but keeps the state change.
- **The feed is CSS Grid, and multi-column must not come back.** Both were tried the same day. Multi-column packs each column independently, so ONE two-line card title pushes every card below it in that column down a line: at 1366px the far-right column's second card sat 21px low, and the same appeared at 1200, 1024, 768 and 414. Reserving two lines of title height fixes that and then **empties the fifth column** -- with sixteen equal-height cards `column-fill: balance` fits them into four columns of four, so [3,3,4,3,3] became [4,4,4,4] and the 5-column band silently became a 4-column one. **The ragged heights were the only reason five columns appeared at all.** Multi-column cannot give aligned rows and five columns at once; Grid gives both and needs no reserved height, because every card in a row shares the row's height. Measured after the switch: zero misaligned rows at all nine test widths and on every category filter. Two bonuses -- reading order now runs across rows rather than down columns, removing a trade-off documented since August 9, and the `.card-category` eyebrow wrapping at 320px is absorbed by row height. `align-items: stretch` and `minmax(0, 1fr)` are both load-bearing; see the CSS comment before touching either.
  - **Masonry was built and then removed the same day, deliberately.** Per-card `.ratio-*` preview ratios gave 8 distinct card heights across a 185px range at 1920px; the owner saw it and asked for it gone, so every preview is back to a single 4/5 and the feed reads as an aligned grid (2 distinct heights across 21px at 1920px). **CSS multi-column is still the layout mechanism** -- only the height variation went, and with it the reason the August 9 rebuild gave for choosing columns over grid in the first place. Two things follow. The residual 21px step is card titles wrapping to one or two lines, not a defect; removing it needs a fixed title height that would clip the longer names. And **reading order still runs DOWN each column rather than across rows**, which is inherent to multi-column and is the one thing a real CSS Grid would have fixed -- keeping the columns was chosen over fixing it. Do not re-add the ratio classes as an oversight: their absence is the decision, and `css/style.css` carries a comment where they used to live saying so.
  - **The column ladder is 5 at 100rem, 4 at 64rem, 3 at 48rem, 2 below.** Counts were verified against the FEED's measured width, never the viewport, because the ad rail takes 184px or 324px out of it from 75rem up. The old `column-count: 1` rule below 26rem is gone: the miniatures were rechecked at 320px in two columns (128.5px cards) and the document wordmarks that identify each template still read, so it was removed rather than quietly kept.
  - **Per-category column counts were asked for and are not buildable in this structure.** The feed is ONE set of CSS columns that the tabs filter in place, so the count is global. Making it per-category means each category becomes its own section with its own column context, the tabs switch sections instead of toggling `.is-hidden`, "All" stops being one continuous feed, and the flat search index splits. That is a change to the feed's model, not a CSS change.
  - **The card title IS the anchor now, and that anchor is load-bearing.** No visible CTA remains, but every card still ships one real `<a href>` with real text, with `data-target` and `data-doc` intact and `.card-link::after` still stretched across the card. **These sixteen anchors are the only crawlable inbound links the four editor pages have** -- the same reason the CTAs were converted from buttons to anchors on July 26. Do not let a later redesign turn them into JavaScript handlers.
  - **`.card-preview` needs `isolation: isolate`, and it is not decoration.** The poster hover's base and overlay carry z-index 2 and 3; with no stacking context on the preview they compete in the CARD's context and paint over `.card-link::after`, so the card looks perfect and refuses clicks across its whole preview. Caught before shipping, but it is exactly the defect that would be reported as "the cards randomly do not work".
  - **The on-site search index shrank, and nothing in the brief raised it.** `initSearch()` indexes `.card-body` text, which included the descriptions. Terms that only ever lived in prose -- `tenant`, `landlord`, `photoshoot` -- now match nothing; titles and category eyebrows still match everything a visitor is likely to type. Left as-is deliberately: the fix would be invisible keyword text, which is the thing the same brief ruled out for SEO.
  - **The card titles are now the only indexable prose on the page.** The H1 is `.sr-only`, and the hero, trust band and keyword strip went on August 9. **If homepage impressions fall in Search Console, this belongs on the list of causes**, alongside those August 9 removals. Nothing was hidden in `.sr-only` or an `aria-label` to compensate, on instruction and correctly.
  - **The poster hover downloads 2.3MB on first hover.** `wood-a4-base.png` is 2.07MB. It is declared only inside the `:hover` rule inside `@media (hover: hover)`, so nothing is fetched until a desktop visitor hovers a poster card and touch devices never fetch it at all -- but it is still 2.3MB on the most-indexed page. A downscaled derivative would end it; that is an added asset, which this pass was told not to create.
  - **All three poster cards hover into the same frame,** because `wood-a4` is the only frame mockup in the repository. No asset paths were invented for frames that do not exist. Two more real frames captured through `tools/mockup-admin.html` is the only honest fix.
- **The ad rail is now WIDER than a feed column at 93rem and up, inverting a documented rule (August 10, 2026).** Flagged rather than resolved, because it is a judgement the owner made once by eye and should make again. The rule, recorded above and in `CLAUDE.md`, is that a 300px unit as wide as a content column "stops reading as a rail and becomes a fourth column of adverts" -- learned from a 1366px laptop, and the reason the 160x600 band exists at all. The new ladder inverts the arithmetic on wide screens: at 1920px the feed's columns are 214px against the stack's 300px creative, and at 1488px they are 257px. **The premise did change underneath the rule**: earlier the same day the rail stopped being an in-flow neighbour of the feed and became a column fixed to the window edge with its own background, which is what separates it from the content now, and beside five dense columns a 300px unit at the window edge still reads as a rail rather than a sixth column. That is an opinion, not a measurement. The 1366px case that produced the original complaint is unaffected -- it sits in the single-unit band, 160px against 262px columns. `tests/verify-layout.js` asserted the old form and failed on it; the assertion was **relaxed to "the ad column takes under a quarter of the window" with the reasoning written into the test, not deleted**. If the wide-screen look is wrong, the fix is the ladder or the band, and the test should be tightened back.
- **A real verification suite exists now: `tests/verify-layout.js` (August 10, 2026).** Written after the discovery that **this document described two test files, `test-home.js` and `verify-site.js`, that were never in the repository.** The guarantees they were credited with were real decisions, and the descriptions were accurate about what should hold — but nothing was checking any of them, and the wording invited every future reader to believe the coverage existed and stop verifying by hand. That is worse than having no tests at all. Every claim of the form "a test asserts X" in this document now points at a check that exists and has been proven to fail when X is broken. Four things worth knowing:
  - **Run it before deploying: `node tests/verify-layout.js` from the repository root.** Nothing runs it automatically; there is no CI. `--quick` runs the static half in under a second, the full run takes about two minutes, exit code 1 on any failure. No dependencies to install — it starts its own `npx serve`, drives a browser already on the machine over the DevTools Protocol, and needs no ad-network access. Full description: `docs/guides/RUNNING_THE_VERIFICATION_SUITE.md`.
  - **Every check was mutation-tested: break the thing, confirm the check catches it, put it back.** This was not ceremony. The selector-hook check originally scanned only class names, passed cleanly, and would have sailed straight past a rename of the `data-filter` attribute — the exact bug it was written for — because the filter handlers key on an attribute, not a class. Nothing but deliberately breaking it revealed that. Likewise the band check originally read "at most one band mounts", which passes when a band silently fails to mount, the failure mode that actually costs money; it had to become an exact count. **An assertion that has never failed is not evidence of anything.**
  - **The launch-flow checks must use trusted input** (`Input.dispatchMouseEvent`), never synthetic `MouseEvent` dispatch. A synthetic event is not a user activation, so the `window.open` in `bindLaunchControls` is popup-blocked and the modified-click checks fail against working code. Two false failures came from this before the cause was found. Same trap for clipboard writes, fullscreen, file pickers and autoplay.
  - **Section 4 compares the working tree against `git archive HEAD` with `js/ads.js` blocked**, which is what makes "a dormant zone changes nothing" an enforced property rather than a hope. A difference there is not automatically a bug — if you deliberately changed a layout, it is supposed to differ — so read the reported values rather than reacting to the count.
- **jsPDF primitives over jspdf-autotable.** The invoice/receipt line-item table, checkbox rows and side-by-side party columns in `js/docs.js` are hand-rolled on `doc.text()`, `doc.rect()` and `doc.line()` rather than adding the autotable plugin. Rationale: it keeps the CDN surface identical to the resume builder's single jsPDF include, and CLAUDE.md mandates the native text API specifically. Do not add autotable to "simplify" the table code without weighing that.
- **~~No ads on index.html, ever.~~ Reversed August 6, 2026.** The original argument, kept here because it is still the reason the *format* is constrained: index.html is the page Google actually indexes (loading.html is noindex,nofollow), so ad clutter there carries real SEO/trust risk the isolated loading-page model does not. What survives of it is that only passive banners in the page's own margin are permitted — **active formats on index.html remain banned outright**. See the ad-inventory note above for the placement as it now stands.
- **Editors carry one passive banner (revised July 27, 2026).** The original rule was "no ads on the editors either, ever". It was reversed on a dwell-time argument: an editing session lasts minutes where a catalog visit lasts seconds, so one passive unit on an editor plausibly out-earns the interstitial per session at a far lower interruption cost. Four mutually exclusive viewport bands (three-slot 300x250 rail at 93rem and above, single 160x600 rail 75-93rem, 728x90 leaderboard 48-75rem, 320x50 anchor below 48rem), never two bands at once and never none, no active format, iframe-isolated only. Each slot in the stack carries its own zone key -- repeating one key is not three placements. The rail gate was 84rem rather than 75rem because below that the page is capped by the viewport instead of its own max-width, so the rail stops using spare margin and starts eating the editing panes — at 1200px it cost each pane 68px against the pre-rail layout. **That gate was lowered to 75rem on August 13, 2026** for parity with the homepage after a 1280x800 MacBook Air showed a rail everywhere except the editors; the cost is real and was accepted rather than solved (panes 465px at 1200px, 505px at 1280px, unchanged from 1366px up), and a max-width cap cannot recover it because the page is viewport-bound in that band. See the "Revised" section of the implementation note. The isolation is what keeps "nothing leaves your device" honest, so **Native Banner and any other page-context format is prohibited on editors**. Full reasoning and rejected alternatives: `docs/implementation/EDITOR_PAGE_AD_PLACEMENT.md`. Four documents were updated to match (`PRD.md`, `CLAUDE.md`, `site/privacy.html`, `site/terms.html`, `site/about.html`) — if this is ever reversed again, all of them need revisiting.
- **Homepage rebuilt as a pure feed (August 9, 2026).** The owner asked for a Pinterest-style interface and, on seeing the first pass, rejected it as a grid tweak rather than an interface change: "i want to completely change the homepage interface". The second pass removed the hero, then in a follow-up the visible H1 masthead, the four-item trust band and the "Popular starting points" keyword strip as well. What is left above the fold is the category tabs and the cards. Three consequences worth knowing before anyone reverses this:
  - **The homepage now has no visible heading and almost no body copy.** The H1 survives as `.sr-only` because Rule 2 in `CLAUDE.md` mandates one per document and this is the most-indexed page on the site; the words are identical to what a visible one would carry, so it is a layout decision rather than a cloaking one. The card titles and descriptions are the page's only remaining indexable prose. **If Search Console impressions for the homepage fall after this ships, this is the first thing to look at.**
  - **The six landing pages the keyword strip linked are not orphaned.** Every content page's footer carries all six and they remain in `sitemap.xml`. This was checked before the strip was deleted; check it again before deleting anything else from this page, because the homepage carries no footer of its own.
  - CSS multi-column, not `grid-template-rows: masonry`, which is still not shipped unprefixed. The accepted trade is that reading order runs down each column rather than across rows — fine for a catalogue with no inherent sequence, wrong for anything ranked or chronological.
  - **`index.html` shipped ad hosts with no `<script src="js/ads.js">` on the page for three days.** The rail and anchor were inert markup, zero impressions, and nothing failed anywhere — every DOM-stub test called `mountHomeAds()` directly, so none of them could see it, and the tag had simply never been needed on a page that carried no advertising for its whole prior life. Reported as "im not seeing the sticky rail add". `tests/verify-layout.js` section 1 now fails any page carrying a `data-ad-*` or `data-ads-static` attribute without that script tag, and cross-checks that every zone named by a `mountPlacement` call exists in `AD_ZONES` with a non-empty key. **The only thing that catches a missing script tag is looking for the script tag.**
- **Reused Adsterra banner keys across both slots is fine** functionally; a second zone was obtained purely for separated reporting, not because sharing was broken.
- **CSP is intentionally not in `netlify.toml` yet** — Adsterra's ad domains rotate/vary enough that a hand-written allowlist would likely break ads; revisit once ad domains are observed to be stable.
- **jsPDF over html2pdf.js** is now the mandated PDF engine project-wide (CLAUDE.md and PRD.md both updated) — do not reintroduce html2pdf.js.
- **Loading-page countdown resilience (July 21, 2026).** The countdown text (`10`) is static markup in `loading.html`, so if `js/app.js` ever fails to load or throws before scheduling its timer, the page freezes at 10 forever with no redirect — indistinguishable from "the countdown isn't working," and distinct from the already-solved stall-at-zero defect in LOADING_REDIRECT_STALL_FIX.md (that one starts the timer but loses the redirect race; this one never starts the timer at all). Two changes close the gap: (1) the `DOMContentLoaded` boot handler in `js/app.js` now runs `initCatalog`/`initLoadingPage`/`initEditorTabs` each in its own `try/catch` instead of as unguarded sequential calls, so a throw in one can't block the others; (2) `loading.html` carries a small inline, dependency-free fallback script (after the `js/app.js` include) that waits 1.5s, checks a `window.__tbLoadingActive` flag `initLoadingPage()` sets on success, and if unset runs its own identical countdown/redirect against its own copy of the route whitelist. Verified with Playwright: normal path completes in 10s as before; with `js/app.js` blocked, the fallback completes in ~12s and lands on the correct editor; a 10-case fuzz of the fallback's whitelist (valid/missing/unknown targets plus six hostile payloads: absolute URL, protocol-relative, path traversal, `__proto__`, `constructor`) all resolved to the correct or default editor with nothing escaping to an external host. Full write-up: `docs/implementation/MOCKUP_GENERATOR_IMPLEMENTATION.md`.
- **Mockup generator (July 20, 2026): flat-vector Canvas products, no image assets.** Rather than photographic mockup templates (which would need licensed/hosted product photos), all four products are drawn as flat vector illustrations directly in `js/mockup.js`, matching the site's "no gradients, no drop shadows" theme and keeping the tool's payload at zero images, consistent with the poster editor's approach. The mug handle's arc deliberately oversweeps past 90 degrees on each side so it tucks behind the body edge instead of floating disconnected — worth knowing before adjusting that shape. The "Add to My Mockups" tray is the closest honest stand-in for "add to products in one click" a database-free tool can offer (there is no real product catalog to attach to); it lives in memory only, not localStorage, for the same image-quota reason `poster.js` never persists the uploaded photo. Full write-up: `docs/implementation/MOCKUP_GENERATOR_IMPLEMENTATION.md`.
- **Business document builder (July 21, 2026): one editor, six catalog cards.** Rather than six near-duplicate pages, `docs.html` drives all six documents from one form and one state object; fieldsets carry `data-for` lists of the document keys they belong to and labels are re-worded from a per-type `labels` map, so the same input reads "Received From (Tenant)" on a rent receipt and "Employee Name" on a warning notice. Hidden fields are still collected into state, deliberately, so switching document types never loses typed data. Three layout families (`receipt`, `itemized`, `notice`) each have one preview renderer and one jsPDF writer. Two things worth knowing before touching it: (1) `CURRENCIES` carries two symbols per entry because jsPDF's built-in fonts are WinAnsi-encoded and cannot draw the rupee/naira/cedi glyphs — those use an ASCII prefix in the PDF only, while dollar/euro/pound/yen are inside cp1252 and print as-is; (2) the PDF table, checkbox and two-column-party primitives are hand-rolled on top of `doc.text()`/`doc.rect()`/`doc.line()` rather than pulling in jspdf-autotable, keeping the CDN surface identical to the resume builder's. The "Blank printable form" toggle is what makes the printable-form cards distinct: empty optional fields render as ruled lines instead of being omitted. Full write-up: `docs/implementation/BUSINESS_DOCUMENT_BUILDER_IMPLEMENTATION.md`.
- **Grid children need `min-width: 0` (July 21, 2026).** `.editor-pane` / `.preview-pane` are CSS grid children and so default to `min-width: auto`, meaning they refuse to shrink below their content's minimum width. The invoice line-item table (four columns, irreducible below roughly 272px) therefore pushed its own pane past its grid track and scrolled the entire page sideways at a 320px viewport — a PRD violation that looked like a table bug but was a grid-sizing default. Fixed with `min-width: 0` on both panes plus an `overflow-x: auto` wrapper on the table, the same containment approach already used on `.ad-slot`. Worth checking first if any editor ever overflows horizontally on mobile.
- **Footer social icons (July 25, 2026): inline SVG, no external assets.** Every public page's `site-footer` carries a `.footer-social` row (between the `.footer-links` nav and the tagline) linking to X (`https://x.com/Templatebox26`), Facebook (`https://www.facebook.com/profile.php?id=61592027191178`), TikTok (`https://www.tiktok.com/@templatebox26`), and Instagram (`https://www.instagram.com/templatebox26/`). Icons are inline SVG paths using `currentColor` (muted, darkening on hover/focus), 2.75rem tap targets matching `.footer-links a`, `target="_blank" rel="noopener noreferrer"`, `aria-label` on each anchor. The block is identical across all nine public pages (admin.html deliberately excluded as a private tool); if the handles ever change, update all nine. Print styles already hide `.site-footer`, so PDFs/prints are unaffected. Verified in-browser at 411px and 320px: no horizontal overflow, no console errors.
- **UI/UX and SEO audit implementation (July 26, 2026).** Both audits are written up in full at `docs/project/UI_UX_AUDIT.md` and `docs/project/SEO_AUDIT.md`, with per-finding status. The decisions most likely to be second-guessed later:
  - **Catalog CTAs are anchors, not buttons.** Every launch control is now `<a href="<editor>.html" data-target="...">`, intercepted by `bindLaunchControls()` in `js/app.js`, which calls `preventDefault()` and routes through `loading.html` exactly as before. Nothing about the monetized flow changed for a human visitor. The reason for the change is that Googlebot does not click buttons, so all four editor pages previously had **zero** inbound internal links from anywhere on the site and were discoverable only via the sitemap. Modified clicks (ctrl/cmd/shift/middle) are deliberately passed through to the browser. Do not convert these back to buttons.
  - **Landing pages exist because one URL cannot rank for six intents.** `docs.html` serves six document types under one title; Google ranks URLs, not features. Nine static landing pages now each own one intent and convert into the editor with the right `data-doc` preset. They were generated once from a template and are now plain static files: edit them directly, there is no generator to re-run.
  - **Loading page copy was dishonest and is now not.** It claimed to be "loading editing canvas, typography fonts, and template assets", which describes work that does not happen (the editors are static pages). That is a dark pattern, it is visible in Clarity session replay, and it bought nothing. The interval, ad count, zone keys and watchdog are all unchanged; only the wording, a progress bar, and naming the chosen template were added.
  - **Blog posts are static files now; `post.html` is a noindex fallback.** See the blog entry below for the full reasoning.
  - **No design-system tooling was introduced.** At one stylesheet, nine pages and one contributor, a synced design-system project would be a second artifact to reconcile by hand while the real work still lands in `css/style.css`. A token layer (`--space-*`, `--motion-*`, `--color-brand`) delivers the substance inside the existing no-build constraint. Revisit past roughly ten tools or a second contributor.
  - **`--accent` and `--color-brand` are deliberately separate.** `--accent` is the per-document colour the resume swatch row overwrites; `--color-brand` is the site's interactive accent. Before this pass `--accent` was `#1A1A1A`, identical to `--color-text`, so outside the resume editor the site had no accent colour at all.
- **Blog moved to static per-post pages (July 26, 2026).** `post.html` resolved every post's title, description, canonical and BlogPosting schema at runtime from `js/blog.js`. Google renders JavaScript but only on a deferred second pass, and **social crawlers never execute JavaScript at all**, so every post shared to Facebook, X, LinkedIn or WhatsApp previewed as the generic "Article | TemplateBox Blog" with no description or image. `admin.html` now has a **Download Post Pages** action emitting `blog/<slug>.html` per post with real metadata in the markup; it builds the body by running the post through the existing `TBBlog.renderBlocks` and serializing the result, so there is still exactly one block renderer in the project and the static output cannot drift. `netlify.toml` 301s `post.html?slug=` to the static file (a redirect rather than a noindex, so already-indexed query URLs pass their signal on instead of losing it), and the admin draft preview moved to `post.html?draft=<slug>&preview=1` so the redirect cannot bounce an unexported draft. **When publishing: export blog-data.js, export the post pages into `blog/`, and add the new URL to `sitemap.xml`.**
- **Deployables moved into `site/`; the publish directory is now the web root (July 27, 2026).** Carried out on request after being deferred earlier the same day. The move is cheap precisely because the publish directory becomes the web root: `site/index.html` is served at `/index.html`, so no canonical, `og:url`, sitemap entry, inbound link or relative asset path needed rewriting. What it buys is that documentation and instruction files cannot be published by accident — a structural guarantee instead of a list of 404 rules to extend every time a note is added. Ten of the eleven rules were deleted; `/serve.json` keeps one because `serve` reads that config from the directory it serves, so it has to ship inside `site/`. Pages stay flat within `site/`, so the reason a `pages/` subfolder was rejected still holds: directory layout is URL layout, and nesting pages would rename every public URL for tidiness alone. Local testing is now `npx serve site`; serving the repository root would break every relative path. Full write-up: `docs/implementation/SITE_PUBLISH_DIRECTORY_MIGRATION.md`.
- **Footer restructured into five columns, applied from one script (July 27, 2026).** "Creative Tools" mixed three landing pages with two of the four editors, which is why `docs.html` and `poster.html` looked arbitrarily omitted. Now: Receipts and Invoices, Resumes and Creative, Editors (all four), Learn, Company. `.footer-cols` is `repeat(auto-fit, minmax(11rem, 1fr))`, so the fifth column needed no CSS. Two drift cases were normalized at the same time: `privacy.html` alone used a `<details>` accordion footer that no CSS rule styles, and the nine landing pages indented the opening `<footer>` tag at column 0 rather than four spaces. All 22 copies plus the `FOOTER` constant in `js/admin.js` were written by one script and verified byte-identical — hand-editing 22 copies is what caused every footer defect so far, so do it that way again.
- **Footer link columns and the guides page are separate systems with zero overlap, by construction (July 27, 2026).** Asked why the footer's pages do not appear on the guides page. The footer is hand-written HTML whose two content columns list the nine landing pages; `blog.html` renders only from `window.TB_BLOG_POSTS` in `js/blog-data.js`, currently one post. No code path joins them, and `blog.html` carries no article markup at all — the list is built entirely in JS behind a `<noscript>` line. Related and more consequential: the **only crawlable link to a blog post was `sitemap.xml`**, because the homepage guides strip (`js/app.js`) and the blog cards (`js/blog.js`) both create their anchors in JavaScript — the same orphaning defect the July 26 pass fixed for the editor pages by converting buttons to anchors. Fixed later the same day with the static guides archive on `blog.html`; the homepage strip is deliberately still a JavaScript render, because it is a "latest three" widget and no longer the discovery path. The footer columns and the archive list remain separate hand-maintained things, which is correct: one is site navigation, the other is generated from post data.
- **Landing pages carry one passive banner, deliberately not in the hero (July 27, 2026).** Asked why the template landing pages had no ads when the hero has visible empty space. That space is a grid artifact, not a slot: the 4:5 preview column is always taller than the headline/CTA column, and `align-items: start` piled the difference under the CTA (now `center`). The hero is also the worst possible placement, because these pages exist to send clicks into `loading.html`, where a Popunder, two 300x250s and the Social Bar run across a ten-second dwell — worth far more than one banner impression from a visitor about to leave. One 300x250 now sits between the body copy and the FAQ, above both the closing CTA band and related templates. Passive format only; Popunder and Social Bar stay off every indexable page. Full reasoning, including the placements rejected and when a second unit would be justified: `docs/implementation/LANDING_PAGE_AD_PLACEMENT.md`.
- **Ad placement extracted to `js/ads.js` (July 27, 2026).** The registry lived inside the 790-line `js/blog.js`; landing pages were the third page family to need it. Loading a content library on nine marketing pages to fill one banner is waste, and a second copy of the zone table is the drift that caused two defects already, so the concern moved to a 187-line dependency-free module (`TBAds`). Auto-mounting is opt-in via `data-ads-static` on `<main>`, which is what stops a renderer-driven page double-counting impressions. Consequence worth knowing: the generated post pages no longer load `js/blog.js` or `js/blog-data.js` at all — neither was read there, and the post database grows with every published post.
- **Blog (July 18, 2026): serverless admin-panel model.** No database means the blog "publishes" by exporting a static `js/blog-data.js` from `admin.html` (drafts live in that browser's localStorage under `tb_admin_posts`) and deploying it. Post bodies are typed blocks rendered createElement/textContent-only — never HTML strings. Blog pages are a monetized surface (banners only, passive formats); the no-ads rule still holds for index.html and the editors, and Popunder/Social Bar are deliberately kept off blog pages because they are indexable content. Ad placements live in the `AD_ZONES` registry in `js/blog.js`: a placement with an empty key renders nothing (zero layout shift), so 728x90/320x50/160x600 activate by pasting keys once Adsterra provisions the zones. Full write-up: `docs/implementation/BLOG_SYSTEM_IMPLEMENTATION.md`.

- **The mockup editor has no template picker; the catalog card is the only selector (July 30, 2026).** The "Product Template" `<select>` was removed at the owner's instruction, completing the Placeit-style flow the brief asked for: pick the mockup, then upload. **This makes a catalog card the only route to a product, so any product without its own card is unreachable** — which is why the same change wired `data-doc` onto all five mockup cards and split "Apparel Mockup: T-Shirt and Hoodie" into separate T-Shirt and Hoodie cards (16 catalog templates became 17). Before it, three of the four vector products had no `data-doc` at all and relied entirely on the picker, so deleting the select alone would have quietly pointed Mug, Packaging and Hoodie all at the T-shirt. The canvas now carries an `aria-label` naming the active mockup, because with the picker gone nothing else identifies which template is loaded. A direct visit with no preset falls back to the last product used, then to the T-shirt. Verified across all five presets plus the no-preset case; the full suite's HEAD-parity check attributes every layout delta to the removed field (a uniform 86.8px at all nine widths) and the extra card. Full write-up: the "The Template Picker Was Removed" section of `docs/implementation/PHOTO_MOCKUP_TEMPLATES_IMPLEMENTATION.md`.
- **Photographic mockup templates (July 30, 2026): registry-driven Sandwich Method inside the existing editor, not a separate gallery app.** The research spec (MOCKUP_CONTEXT.MD at the repo root) described a Mockey/Placeit-style gallery page with an editor modal; that shape was deliberately not built because index.html already is the gallery and a same-page modal would bypass the mandatory loading.html ad interstitial. Each photographic template is a catalog card (`data-target="mockup" data-doc="<id>"`) using the same preset hand-off docs.html uses. Compositing order is declared per template because the purchased asset style has a fully transparent print window in the base: mode "window" draws design first (over a white paper backing), then the base whose antialiased window edge masks it, then the overlay; mode "surface" is the classic base-first order for opaque assets. **The overlay's blend mode is also per-template (`overlayBlend`) and it is the single most consequential field**: this asset's overlay is a baked luminance map (alpha 193 over a near-white body inside the window), so at the canvas default `source-over` it painted over the artwork and desaturated it completely — a design colour of (196,138,74) sampled as (234,219,204). At `multiply` it samples (190,134,72) and the window-light and pampas shadows fall across the design convincingly. Canvas silently ignores an unrecognized composite operation and leaves `source-over`, so the value is whitelisted in code; a typo would otherwise reintroduce the washout with no error anywhere. See `docs/error-fixes/PHOTO_MOCKUP_OVERLAY_WASHES_OUT_DESIGN.md`, which also records how to pick the mode from an asset's appearance. The warpZone is stored as four corners so angled frames slot in later: rectangular zones use the existing contain/drag/scale 2D path, non-rectangular zones will lazy-load the vendored glfx.js and map corner-to-corner (that path is dormant and unverified until a real angled asset exists — the cdnjs glfx URL in the research notes is a 404, which is why the library is vendored from jsdelivr instead of CDN-linked). Coordinates for wood-a4 came from an alpha-channel scan of the base (window: x 655-1461, y 224-1583 in 2000x2000); future templates use tools/mockup-admin.html. Asset names must be URL-safe — the supplied thumbnail folders ("Product Mockups/Posters,frames,canvas, billboards") contained spaces and commas and were renamed to kebab-case before anything could reference them. Full write-up: `docs/implementation/PHOTO_MOCKUP_TEMPLATES_IMPLEMENTATION.md`.

## Extending TemplateBox: Adding a New Template/Editor

Pattern demonstrated by resume.html/docs.html/poster.html/mockup.html, to follow for a fifth tool:

1. Add a card to the `.catalog-grid` in `index.html`. The CTA must be an **anchor** — `<a class="btn btn-block card-link" href="yourpage.html" data-target="yourkey">` — not a button, or the new editor page will have no crawlable inbound link (see the July 26 decision log). Give the card a `data-category` matching an existing or new filter pill, and a filled `.mock-doc` miniature rather than an empty preview. If the new editor serves several cards, also give each card a `data-doc="variantkey"` and consume it with `TB.takePreset()` (see the docs.html pattern above).
2. Add `yourkey: "yourpage.html"` to `EDITOR_ROUTES` in `js/app.js`, to the `EDITORS` registry beside it (label plus the localStorage key the editor writes, which powers the continue strip and the loading page's template name), **and** to the mirrored inline `ROUTES` table in `loading.html` — this is what keeps the loading-page redirect safe from tampering, on both the normal and the fallback path.
3. Build `yourpage.html` + `js/yourpage.js` following the resume/poster split-pane pattern: `.editor-layout` / `.editor-pane` / `.preview-pane` / `.mobile-tabs` CSS classes are already themed and responsive; `TB.sanitize`, `TB.desanitize`, `TB.storageGet`, `TB.storageSet` from `js/app.js` are the shared utilities to reuse for localStorage binding and XSS-safe rendering. Also include the shared editor furniture: `.breadcrumb`, `.editor-head` with `<p class="save-state" id="save-state">`, the export control inside `.preview-actions`, and a `.related-tools` block. Call `TB.markSaved()` after every `TB.storageSet` so the autosave indicator reflects reality.
4. No ad work needed — the new tool automatically routes through the existing monetized `loading.html` flow.
5. Add SEO meta tags, canonical URL, Twitter Card tags, and `SoftwareApplication` + `BreadcrumbList` + `Organization` JSON-LD following the pattern in resume.html/docs.html. Add the new page to `sitemap.xml` with a `lastmod`, and add it to the appropriate footer column — a landing page goes in "Receipts and Invoices" or "Resumes and Creative", an editor in "Editors". The footer is byte-identical across all 22 pages plus the `FOOTER` constant in `js/admin.js`, so change it by generating the block once from a script and writing it to every copy, not by hand-editing 22 files (see the July 27 decision entry).
6. If the new tool serves a distinct search intent, give it a landing page too, following the shape of `rent-receipt-template.html`: own title/H1/canonical, real body content, `FAQPage` schema, and a CTA into the editor with the right preset.

## Open Items

- **Tablets carried no advertising at all, and the test suite asserted that was correct (August 20, 2026).** Reported as "there are no ads on the tablet screens". Every non-editor page family -- the homepage, the nine landing pages, `about`/`terms`/`privacy`, `blog.html`, `404.html` and `loading.html` -- showed nothing at all from 769px to 1199px. That is every tablet in portrait and most in landscape. Three separate things had to be true at once for it to survive this long, and the third is the one worth remembering:
  - **The anchor's ceiling and the rail's floor did not meet.** `SITE_ANCHOR_MAX` and `HOME_ANCHOR_MAX` stopped at 48rem while the rails start at 75rem. The August 13 entry retired the anchor's desktop half on the reasoning that the rail "takes over once there is room for it", and explicitly accepted the leftover gap as matching the homepage's own -- but the homepage's gap was the same defect, not a precedent for it. Both now end at **74.9375rem**, one pixel below the rail's floor, so the two bands meet exactly and no viewport mounts both or neither. Verified across 132 page/width rows: **zero rows with two bands**, and the only rows with none are `404.html` above 1200px, which is pre-existing (it was deliberately left out of the content-rail rollout).
  - **`loading.html` showed one banner where it has two zones.** `#ad-banner-2` was hidden below **70rem**, so the whole tablet band got a single 300x250. The reason for cutting it was always vertical -- a second banner *stacked* under the card pushes a short phone into scroll -- and that reasoning stops applying the moment the two fit side by side. `.ad-row` is flex-wrap, so from 769px up the pair share one row (300 + 300 + 24px gap = 624px, inside even the narrowest tablet) at the same 250px of height one banner costs. The gate is 48rem now, so phones still get one. Measured with the anchor's reservation included: **zero vertical scroll** at 800x1280, 810x1080, 820x1180, 1024x768, 1112x834, 1180x820 and 1199x800, preserving the page's zero-scroll invariant. 768x1024 is 48rem exactly and correctly still shows one.
  - **The suite defended the hole.** `RAIL_GAP` in `tests/verify-layout.js` listed `index`, `about`, `rent-receipt`, `blog` and `post` at `[768, 1200]` and asserted **"exactly 0 ad band mounts"** there. The band-count check was doing its job perfectly against a table that encoded the defect as intended behaviour. `RAIL_GAP` is empty now. **This is the second time a check in this project has been correct about the wrong thing** -- the first was the band assertion that read "at most one band", which passes when a band silently fails to mount. A check is only as good as the expectation it encodes, and an expectation table is exactly where a stale design decision hides.
  - **A dead CSS branch came back to life and over-reserved.** `body.has-site-anchor` reserved `7.25rem` (116px), sized for the 728x90 the anchor used to mount above 48rem. That unit was retired on August 13, which made the branch unreachable -- the anchor's own gate stopped at 48rem, so only the 4.75rem override could ever apply. Extending the gate to tablets resurrected it instantly: **116px of padding under a 50px bar** on every tablet page, with nothing failing anywhere. `mountSiteAnchor` only ever mounts `leaderboardMobile`, so it is one 4.75rem value now. A new check asserts the reservation matches the mounted unit's real rendered height in both directions, and is mutation-proven (it reports `padding-bottom 116 vs anchor height 75.4`). **Retiring a unit should have deleted the rule sized for it**; leaving it as unreachable code meant the next gate change shipped a regression for free.
  - **The editors and the unit size were both flagged as open decisions, and both were then decided the same day -- see the entry directly below.** As first shipped the editors kept their in-flow 728x90 tablet band and the anchor mounted a 320x50 everywhere; neither of those is true any more.

- **The anchor is size-tiered, and the editors' tablet band is now the fixed anchor too (August 20, 2026, second pass).** Both were flagged as owner decisions when the tablet hole was closed, and both were then decided: "do the same on editor pages of tablet screens", and "shouldn't the anchor ads be bigger on tablet screens?".
  - **One function decides the unit now.** `mountAnchorUnit()` in `js/ads.js` mounts `leaderboardMobile` (320x50) at or below `ANCHOR_PHONE_MAX` (48rem) and `leaderboard` (728x90) above it, and **every** anchor on the site -- site-wide, homepage and editor -- goes through it. That single breakpoint is what the CSS reservations mirror, so the reserved height cannot drift from the mounted creative. Verified at the boundary: 768px mounts 320x50, 769px mounts 728x90, and a 728px unit clears a 769px viewport with zero horizontal overflow (`.site-anchor` has no horizontal padding). **It deliberately has no fallback to the other size**, unlike `mountLeaderboard` -- a fallback would mount a creative the CSS has not reserved for, and mounting nothing is the safer failure.
  - **The editors' in-flow leaderboard band is retired.** `js/ads.js`'s own comment had always conceded the flaw in it: "It scrolls away, which is why it is the fallback rather than the primary: the whole argument for advertising on editors is session-long visibility." A unit that scrolls out of a session measured in minutes contradicts the only argument for advertising on an editor at all. The same 728x90 creative is now in the fixed anchor instead, so it persists for the whole session. **The editors have three bands now, not four**, and `CLAUDE.md`'s four-band wording was updated rather than left to rot. `[data-ad-editor-leaderboard]` is no longer mounted by anything; the host stays in the four editors' markup, empty and collapsed, so restoring the band is a one-line change rather than an edit to four pages.
  - **The export bar overlapped the anchor, and the phone tier's arithmetic did not carry over.** `.preview-pane` is `position: sticky` only above 48.0625rem, and with a sticky pane the export bar's stuck position lands a consistent **12px below** what its `bottom` nominally asks for -- measured 664 against an arithmetic 652 at 1024x768, and the identical 12px at 769px and 1199px. Below that breakpoint the pane is not sticky and the offset is exact (724 against 724 at 400x800), which is why the phone tier needs no allowance and **must not copy the tablet one**. At the reservation's own 7.25rem the bar finished 11.4px inside the 115.4px anchor on all four editors at every tablet width, with the anchor's z-index 30 painting over the export control -- a direct violation of `CLAUDE.md`'s "the banner never overlaps the sticky export bar". The tablet lift is 8.25rem, clearing by 4.6px, and a new check asserts it.
  - **A self-inflicted one worth remembering: the ad label is part of the height arithmetic.** Scoping the small disclosure label to phones only -- so the 90px tablet unit could carry a normal-sized label -- looked harmless and took the bar to 122.6px against a 116px reservation, which is what first put the export bar inside the anchor. The reservation, the export-bar lift and the label size are one system; changing any one of them requires re-measuring the other two. The label is unscoped again, matching `.site-anchor`, which has always had it that way.
  - Suite is 980 passing, 0 failing. Three new checks across the two passes -- reservation-matches-unit, export-bar-clears-anchor, and the band count that now expects 1 instead of 0 on tablet -- all mutation-proven.

- **Responsive audit across mobile, tablet, laptop and desktop (August 20, 2026).** Seven areas were examined; two were real defects and are fixed, three were verified correct and closed, two remain open below. Full write-ups: `docs/error-fixes/CATEGORY_TABS_HIDDEN_BEHIND_HEADER_ON_PHONES.md` and `docs/error-fixes/IOS_SAFARI_AUTOZOOM_ON_EDITOR_FIELDS.md`. Suite went 881 to 902 checks, all passing.
  - **FIXED: the category tabs were painted over by the header on every phone width.** `docs/memory/PROJECT_STATUS.md` recorded this as the tabs "only partially closing the gap" on the narrowest phones. That description was wrong, and the wrong description is why it sat open: there is no gap, the tabs were *behind* the header. `.site-header` is `flex-wrap: wrap`, so its height is a function of how its contents wrap, not of viewport width -- measured 201px at 320px and 145px from 360px to 480px, against the 76px `.feed-tabs` reserved. The whole 45px tab row sat inside the header's box, and the header (z-index 20 against the tabs' 15) painted over it, so category filtering was invisible and untappable on any phone whenever the header was showing and the page was scrolled. `elementFromPoint` at the tab row's centre returned an element inside `.site-header` at 320/360/390/414px. Fixed by publishing the header's measured height as `--header-h` from a `ResizeObserver` (`initHeaderHeight()` in `js/app.js`) and having every offset read it; the old literals stay as `var()` fallbacks so a JavaScript failure degrades to the previous behaviour rather than a broken one. Overlap is now 0.0px at all eight tested widths, the hit test returns a real `a.tab` on phones, and the August 14 "tabs land flush at y=0" property still holds everywhere. **Why the August 14 pass missed it:** that pass measured the header-*hidden* state, where the sticky offset and the hide transform were the same literal and cancelled exactly. The broken state is the opposite one -- header revealed while the page is still scrolled -- and it was never measured. The same fix closed a 9px overlap at 600-768px and the 1px desktop overlap this document already recorded.
  - **FIXED: 27 editor fields were small enough to trigger iOS Safari's focus zoom.** Carried from the PRD mobile checklist as "confirm no input auto-zoom" and never actually measured. iOS zooms the viewport in when a text-entry field computes under 16px and does not zoom back out on blur -- once per field, on pages whose whole purpose is filling fields in. Measured at 390px: `.doc-name` at 15px on **all four** editors, the poster text/Advanced/Download toolbars at 13px, the mockup colour and scale inputs at 12-13px; twelve are on screen without opening anything. The `.field` inputs the main resume and docs forms use were already 1rem, which is why this never showed. Fixed with one phone-scoped 16px rule. **The viewport meta tag was never at fault** (verified: every page ships `width=device-width, initial-scale=1.0` with no scale lock), and `maximum-scale=1` is the wrong fix -- it kills pinch-zoom and fails WCAG 1.4.4. Reflow was the real risk and was measured, not assumed: the full 168-combination overflow sweep is still zero after widening those controls, and the only HEAD-parity difference in 1260 measurements is the poster toolbar standing 4px taller at 320px, which is the intended consequence.
  - **VERIFIED CORRECT, no change: the ad bands.** All twelve page families at eleven widths. Exactly one band mounts at every width, never two and never none where one is due; the boundaries land exactly on 768/769, 1199/1200 and 1487/1488; `body`'s reserved `padding-right` equals the mounted column's width to the pixel (184px single, 324px stack); and the header's right edge equals the rail's left edge **exactly** at every rail width on every page. The documented 769-1199px dead band on the homepage, landing, legal and blog pages is present and correct, as is the editors' leaderboard filling it. Worth knowing for future testing: `js/ads.js` reads `matchMedia` once at mount and deliberately never re-mounts, so a band can only be tested with a **fresh page load**, never by resizing.
  - **VERIFIED CORRECT, no change: horizontal overflow.** Twelve page families at 320, 360, 390, 414, 768, 769, 1024, 1199, 1200, 1280, 1366, 1440, 1488 and 1920px -- 168 combinations, zero overflow, before and after the font-size change.
  - **VERIFIED, partially: touch input.** Both canvases carry `touch-action: none` and use Pointer Events with `setPointerCapture`, which is the correct construction and the reason a drag cannot scroll the page. **mockup.html's design drag is now empirically confirmed under real touch input for the first time** (open since July 20): with a design uploaded through the production handler, a dispatched touch drag moves the artwork and the page does not scroll. **poster.html's text drag remains unverified** -- see the open item below.
  - **STILL OPEN: the 75-84rem editor pane squeeze was left alone deliberately.** Re-measured and unchanged from what the August 13 entry records. It is a knowingly accepted cost, not a defect, and nothing here touched the 75/84/93rem arithmetic.

- **Touch targets under 44px on the two canvas editors (found August 20, 2026, not fixed).** Measured at 390px, counting only controls that are genuinely small: poster's text toolbar is fourteen controls at 32x32 or 27px tall (bold, italic, underline, strike, case, undo, redo, and the font/align/list/anchor selects), mockup has `m-add-design` at 28x28 and `m-scale-reset` at 32x32, and all four editors carry a 36x36 `.editor-home` link. The docs builder's checkboxes measure 13x13, though each has an associated `<label>` that is itself tappable, so the effective target is larger -- worth confirming rather than assuming if that page is ever reworked. **Two categories in the raw measurement are false positives and should not be chased:** the homepage `.card-link` anchors measure 21-42px tall but `.card-link::after` is stretched across the entire card, so the real target is the whole card; and the category tabs measure narrow but are a full 44px tall, which is the axis that matters in a horizontal scroller. The genuine cases are a density decision on toolbars designed for a mouse, not a layout fault, so they are recorded rather than unilaterally widened -- enlarging fourteen toolbar controls changes how that toolbar reads at every width.

- **Section 4 (HEAD parity) is currently unreliable in this environment, and its differences must not be trusted without this test (August 20, 2026).** It was reporting two differing measurements on `poster @320`, which was read as the intended consequence of a deliberate change -- a reasonable reading, and wrong. **Proved by stashing every working-tree change so the tree was byte-identical to HEAD and running it again: it still reported differences, and completely different ones** -- `docs @1600` with `main` at 5087.2px against HEAD's 2804.3px, a 2283px gap that no CSS change could produce and that cannot exist between two identical trees. The section compares two browsers driven against two servers, and when one settles in a partially-rendered state the comparison reports the difference as a layout change. Same root cause as the nav timeouts below: this machine cannot reach the ad hosts, so `readyState` and resource settling are both timing-dependent.
  - **Consequence: a section 4 difference is not evidence of anything right now**, in either direction -- it can report noise as a change, and could equally mask a real one. The "455 measured properties, 0 differences" and "1260 measurements" guarantees quoted elsewhere in this document and in `FIXED_FULL_HEIGHT_AD_RAIL.md` were sound when written but are not currently reproducible here.
  - **The fix is to stop waiting on `readyState === "complete"`** -- which waits on unreachable ad iframes -- and instead poll for the specific elements each comparison measures, the way `navigate()` already polls for `TBAds`. Until then run `--no-baseline` and verify deliberate layout changes with targeted measurement, which is what was done for both August 20 passes.

- **`tests/verify-layout.js` intermittently dies on a navigation timeout, and it is not the page it names.** Seen three times on August 20, 2026 naming `/post.html`, `/resume.html` and `/` on different runs, always `did not settle within 20s`, with runs on either side of each failure passing the same page. The suite's own `navigate()` comment explains the mechanism: the main-frame load event waits on ad iframes whose script host is unreachable from a test machine, so `readyState` reaches `complete` only once those connections fail, and how long that takes is ambient. It got materially worse while a second `npx serve` and several leftover headless browsers were running, and cleared once those were stopped -- **`proc.kill()` in the harness reaps the shell, not the browser's renderer processes**, which is the same reaping problem `startServer`'s `killTree` comment already documents for the server. Treat a lone nav timeout as noise and re-run before investigating; if it becomes frequent, the fix is to stop waiting on `complete` and poll for the specific elements each section needs.

- **poster.html's text drag could not be exercised in the headless harness, and is still unverified.** Attempted during the August 20 responsive audit and abandoned honestly rather than reported as passing. The default text element sits at `y: 0.88` of the canvas, not its centre, so a drag from the middle misses `hitTest` legitimately; but a drag aimed at 0.88 did not move it either, and **neither did a mouse drag at the identical coordinates**. That control is what establishes this as a harness limitation rather than a touch defect -- had only the touch drag failed, it would have been a real bug. Locating the text by scanning the canvas for ink does not work either, because the poster ground is not white and the scan matches the whole canvas. Verifying this properly needs either a hook exposing the text element's rendered bounds or a real device. Do not record poster touch-drag as working until one of those happens.

- **Real-touchscreen and real-device passes for mockup.html**, still open from July 20 and not addressed by the July 30 work: the pointer-drag interaction on a touchscreen, and a narrow-viewport check on a real device. (A 320px headless capture shows right-edge cutoff on every editor including `docs.html`, which was Playwright-verified clean on July 21 — that is headless-Edge window clamping, not evidence of overflow, so do not chase it as a layout bug.) Note that **file uploads can now be verified headlessly**: build a `File` with `canvas.toBlob`, assign it to the input through a `DataTransfer`, and dispatch `change` — the production handler runs unmodified. That technique closed the upload-verification gap on July 30 and applies equally to `poster.html` and the resume builder's photo field.
- **Perspective-warp path is dormant until the first angled asset.** `zoneIsRect()` routes every current template through the plain 2D path; the glfx branch has never rendered a real image. Verify it visually when the first non-rectangular warpZone template is added.
- **Confirm the article-page ad zones start filling.** Four zones served nothing until today's fix (see STATIC_POST_PAGES_SERVED_NO_ADS.md), so their impression counts are a clean before/after: leaderboard `7577a9ab`, in-content `4a408738`, end-of-article `70d844a3`, skyscraper `aaa51e99` should go from zero to non-zero on the Adsterra dashboard a day or so after deploying. A first browser load showing nothing is not evidence of a fault -- frequency capping and zone warm-up both apply (see SOCIAL_BAR_NOT_DISPLAYING.md).
- **Post-deploy verification of the `site/` move.** Nothing here can be checked locally, because `netlify.toml` is only read by Netlify. After deploying: confirm the publish directory took effect (deploy log file count should match `find site -type f | wc -l`); confirm `/`, `/404.html`, `/blog.html`, `/blog/<slug>.html`, `/css/style.css`, `/robots.txt`, `/sitemap.xml` and one landing page all return 200 at their unchanged URLs; confirm `/docs/memory/PROJECT_STATUS.md`, `/PRD.md`, `/CLAUDE.md` and `/serve.json` return 404 (nothing internal is in the deploy at all now, so these need no redirect rules); confirm a mistyped URL renders the new `404.html` rather than Netlify's default. Commands are in `INTERNAL_FILES_PUBLICLY_SERVED.md`.
- **Browser pass on this pass's changes.** Playwright is not installed here, so the five-column footer (at 320px in particular, where `auto-fit` should collapse it to a centered stack), the guides archive list, and the `404.html` layout were verified structurally and against the existing CSS tokens, not visually.
- **`site/404.html` is not in `sitemap.xml` and must stay out of it**, and it has no canonical by design. Worth remembering before any bulk sitemap regeneration.
- Mobile device pass from the PRD checklist: ~~confirm no input auto-zoom~~ (done August 20, 2026 -- 27 fields were under 16px and are fixed; see the responsive-audit entry at the top of this section), ~~ad slots stack cleanly under 768px~~ (done August 20: exactly one band mounts at every width on every page family, boundaries exact), localStorage restores correctly, all on a real device over mobile data (also gives an unfiltered view of ad rendering). The two remaining halves genuinely need hardware: headless emulation cannot speak to localStorage persistence across a real app switch, and it never sees a real ad fill.
- Sitemap fetch status in Search Console — expected to self-resolve, not currently blocking anything (homepage is already indexed).
- Formal UI/UX and SEO audits completed July 26, 2026 (`docs/project/UI_UX_AUDIT.md`, `docs/project/SEO_AUDIT.md`) and implemented. Remaining open items carried from them:
  - **Generate the remaining Open Graph cards.** `tools/og-image.html` carries a preset per public page (own headline, document illustration and filename); the six document illustrations are drawn with Canvas primitives in the same flat-vector style as `js/mockup.js`, so the tool needs no image assets. Three cards exist so far, all 1200x630 and roughly 90-100KB: `og-resume.png` (wired to `ats-resume-template.html` and `resume.html`), `og-rent-receipt.png`, `og-warning-notice.png`. **`og-cover.png` is the priority remaining one** — it is the site-wide default, so until it exists `index.html` and every unwired page still shares a 489KB square logo that platforms crop to their 1.91:1 frame.
  - **`og:image:width`/`height` were removed from the nine pages still on `logo.png`.** They declared 1200x630 for an image that is actually 1219x1509. Platforms use the declared size to reserve the preview frame before the file downloads, so a wrong pair renders the card badly, whereas a missing pair only costs a measuring round trip. Pages wired to a real card keep an accurate declaration; re-add the pair whenever a page is pointed at a generated 1200x630 card.
  - **Resubmit the sitemap in Search Console** after deploying, so the nine landing pages, About, Terms and the static blog page enter the crawl queue.
  - **Content programme.** One published blog post is not a content strategy; the target query clusters are tabulated in `SEO_AUDIT.md` section 5.
  - **Font self-hosting decision.** Fonts now load non-render-blocking via the `media="print"` swap, but still from a third-party origin. Self-hosting the woff2 files would remove the dependency entirely; it introduces binary assets and an attribution obligation, so it was left as a decision rather than assumed.
  - **Persistent mobile preview.** Below 48rem the tabs still fully hide the live preview while typing, which switches off the core value proposition on the traffic segment most likely to dominate. A collapsed preview strip or floating affordance needs validating on a real device before committing to a pattern. **Looked at again on August 20, 2026 and deliberately not built**, because the bottom edge of a phone editor is already fully committed and that is where every "peek strip" pattern wants to live: the fixed 320x50 ad anchor owns it, `.has-ad-anchor` reserves 76px of `body` padding for it, and the sticky export bar is lifted to sit directly above that. A third stacked band there would leave roughly a third of a 320x568 viewport for the form the visitor came to fill in. The honest options are a top-anchored mini-preview under the header (the one free edge, and now straightforward since `--header-h` exists), or a full-screen preview overlay that returns to the form at its previous scroll position -- which is close to what the existing tab switcher already does. Both change the mobile editing model on all four editors, which is an owner call on product, not a responsiveness bug, so it stays open rather than being shipped inside a layout pass.
  - **Browser verification of this pass.** All 23 pages were verified structurally (internal link integrity, 40 JSON-LD blocks parsed, single-H1, canonical and metadata coverage, sitemap coverage, duplicate IDs) and served locally with every asset returning 200, but no browser-rendered pass was run: Playwright is not installed in this environment. Worth a visual check of the filled catalog miniatures at 320px, the sticky export bar, and the first-run sample notice.
- Browser pass on admin.html and the under-48rem mobile stack of the blog pages (desktop blog.html and post.html verified July 18, 2026 with 300x250 banners filling; leaderboard/skyscraper zones added the same day, first fill not yet observed).
- `docs.html` (added July 21, 2026) was verified headlessly via Playwright the same day: catalog filtering, the card-to-editor variant hand-off, per-type field visibility and relabeling, totals arithmetic, amount-in-words, currency switching (including the zero-decimal JPY path), an XSS probe, reload persistence, PDF export for all six document types (single page, real `Tj` text operators, zero image XObjects, nothing drawn outside page bounds), and zero horizontal overflow at 320px across all three layouts, all with zero console errors. Still open: a real-touchscreen pass and a check of actual browser print-dialog output (the print rules were verified as CSS only, not as a rendered print job).
- `mockup.html` (added July 20, 2026) was verified headlessly via Playwright the same day: all four product illustrations render correctly (including the mug handle attaching cleanly to the body), color swatches, design upload/clip, mouse-drag repositioning, and the tray all work with zero console errors, at desktop viewport size. The full catalog -> loading.html?target=mockup -> countdown -> mockup.html flow was also verified end to end (see LOCAL_SERVE_CLEAN_URL_DROPS_TARGET_QUERY.md for a local-testing-only redirect issue hit and fixed along the way). Still open: a real-touchscreen pass for the pointer-drag interaction, and a narrow-viewport (down to 320px) check of the mobile tab layout and tray grid.
